# Test Cases — Pantip Crawl Post From Category

> Template: 7 cột theo chuẩn (TEST CASE ID, MODULE/FEATURE, TEST NAME, PRE-CONDITION, TEST STEPS, TEST DATA, EXPECTED RESULT)

---

## A. LOADER — Load Source từ MySQL & Redis

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PtCatPost_001 | Loader | [High] [Positive] Loader lấy đúng danh sách source từ MySQL với state=IDLE và first_page_next_crawl_time đã tới | 1. Bảng `monitor_news_categories` có ít nhất 1 record với `state='IDLE'` và `first_page_next_crawl_time <= NOW()`. 2. Service Loader đang chạy. 3. Redis chưa lock source. | 1. Kiểm tra bảng `monitor_news_categories` có record thỏa điều kiện. 2. Chờ Loader chạy theo cycle `*/15 * * * * *`. 3. Kiểm tra queue `cl.pt.category_posts_crawling_sources` trong RabbitMQ. | Record: `id=439882, link='https://pantip.com/forum/food', state='IDLE', first_page_next_crawl_time='2026-05-14T09:52:32'` | 1. Loader query đúng record `id=439882`. 2. Message được publish vào queue `cl.pt.category_posts_crawling_sources`. 3. Message chứa `categoryInfo.id=439882`, `categoryInfo.link='https://pantip.com/forum/food'`, `categoryInfo.platform=14`. |
| TC_PtCatPost_002 | Loader | [High] [Negative] Loader bỏ qua source có state khác IDLE | 1. Bảng `monitor_news_categories` có record với `state='RUNNING'` hoặc `state='STOPPED'`. 2. Service Loader đang chạy. | 1. Update record: `UPDATE monitor_news_categories SET state='RUNNING' WHERE id=439882`. 2. Chờ Loader chạy theo cycle. 3. Kiểm tra queue `cl.pt.category_posts_crawling_sources`. | Record: `id=439882, state='RUNNING', first_page_next_crawl_time='2026-05-14T09:52:32'` | 1. Loader KHÔNG publish message cho `id=439882`. 2. Queue không có message với `categoryInfo.id=439882`. |
| TC_PtCatPost_003 | Loader | [High] [Negative] Loader bỏ qua source có first_page_next_crawl_time chưa tới | 1. Bảng `monitor_news_categories` có record với `state='IDLE'` nhưng `first_page_next_crawl_time > NOW()`. | 1. Update record: `SET first_page_next_crawl_time = DATE_ADD(NOW(), INTERVAL 1 HOUR)`. 2. Chờ Loader chạy. 3. Kiểm tra queue. | Record: `id=439882, state='IDLE', first_page_next_crawl_time=NOW()+1h` | 1. Loader KHÔNG publish message cho source này. 2. Source chỉ được crawl khi `first_page_next_crawl_time <= NOW()`. |
| TC_PtCatPost_004 | Loader | [Medium] [Positive] Loader chỉ lấy source thuộc platform PANTIP (id_table=14) | 1. `monitor_news_categories` có records thuộc nhiều platform (14=Pantip, khác). 2. Config `includeTableIds: "14"`. | 1. Thêm record với platform khác (VD: `platform=1`). 2. Chờ Loader chạy. 3. Kiểm tra queue chỉ có message platform=14. | Records: `id=439882 (platform=14)`, `id=999 (platform=1)` | 1. Chỉ message có `categoryInfo.platform=14` được publish. 2. Record platform=1 bị bỏ qua. |
| TC_PtCatPost_005 | Loader | [Medium] [Negative] Loader bỏ qua source đã bị lock trong Redis | 1. Source `id=439882` đã tồn tại lock key trong Redis. 2. Loader đang chạy. | 1. Set Redis key lock cho source: `SET lock:source:439882 1`. 2. Chờ Loader chạy. 3. Kiểm tra queue. | Redis key: `lock:source:439882 = 1` | 1. Loader phát hiện source đã lock. 2. KHÔNG publish message cho source này. 3. Log ghi nhận source bị skip do lock. |
| TC_PtCatPost_006 | Loader | [Medium] [Positive] Loader publish message chứa đầy đủ delay_time_rules | 1. Config có `delayTimeRules` với 3 rules. 2. Source IDLE sẵn sàng. | 1. Chờ Loader chạy. 2. Lấy message từ queue. 3. Kiểm tra field `delay_time_rules`. | Config: `delayTimeRules: [{lte:72, delay:18}, {lte:144, delay:24}, {lte:999999999, delay:48}]` | 1. Message chứa `delay_time_rules` = `[{lte:72, delay:18}, {lte:144, delay:24}, {lte:999999999, delay:48}]`. 2. Đúng 3 rules theo config. |
| TC_PtCatPost_007 | Loader | [Medium] [Edge] Loader giới hạn tối đa 100 source mỗi lần query | 1. `monitor_news_categories` có >100 records thỏa điều kiện (state=IDLE, time đã tới). | 1. Insert 150 records thỏa điều kiện vào DB. 2. Chờ Loader chạy 1 cycle. 3. Đếm số message trong queue. | 150 records với `state='IDLE'`, `first_page_next_crawl_time <= NOW()` | 1. Chỉ tối đa 100 message được publish trong 1 cycle. 2. 50 records còn lại sẽ được xử lý ở cycle tiếp theo. |
| TC_PtCatPost_008 | Loader | [Low] [Edge] Loader kiểm tra maxMsgInQueue trước khi publish | 1. Queue `cl.pt.category_posts_crawling_sources` đã có gần 50000 message (maxMsgInQueue). | 1. Flood queue tới 50000 messages. 2. Chờ Loader chạy. 3. Kiểm tra Loader có publish thêm không. | Queue size: 50000 (= maxMsgInQueue) | 1. Loader kiểm tra số message trong queue. 2. Nếu >= 50000, Loader KHÔNG publish thêm. 3. Chờ queue giảm rồi mới publish tiếp. |

---

## B. BUILDER — Build Request Crawl

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PtCatPost_009 | Builder | [High] [Positive] Builder consume message từ crawling_sources và build request đúng format | 1. Queue `cl.pt.category_posts_crawling_sources` có message. 2. Builder service đang chạy. | 1. Publish message vào queue `cl.pt.category_posts_crawling_sources`. 2. Chờ Builder consume. 3. Kiểm tra queue `cl.pt.category_posts_crawling_requests`. | Message source: `{id:439882, categoryInfo:{id:439882, link:'https://pantip.com/forum/food', platform:14}, delay_time_rules:[...], createdBy:'PtCategoryPostsCrawlingLoader'}` | 1. Builder consume message thành công. 2. Message mới trong queue `cl.pt.category_posts_crawling_requests` chứa 2 phần: `sources` (thông tin source) và `request` (request crawl). 3. Field `createdBy = 'PtCategoryPostsCrawlingLoader'`. |
| TC_PtCatPost_010 | Builder | [High] [Positive] Builder build đúng from_date và to_date | 1. Source có `last_data_date` từ lần crawl trước. | 1. Publish message có `last_data_date='2025-05-22T04:20:16.024Z'`. 2. Chờ Builder xử lý. 3. Kiểm tra message output. | `last_data_date='2025-05-22T04:20:16.024Z'` | 1. `from_date` được tính từ `last_data_date` (epoch: `1747887616`). 2. `to_date` được tính hợp lý (epoch trong tương lai). 3. Khoảng thời gian crawl phù hợp. |
| TC_PtCatPost_011 | Builder | [Medium] [Edge] Builder xử lý source lần đầu crawl (không có last_data_date) | 1. Source mới, chưa crawl lần nào. `last_data_date` = null. | 1. Publish message với `last_data_date=null`. 2. Chờ Builder consume. 3. Kiểm tra request output. | `{id:439882, last_data_date:null, categoryInfo:{...}}` | 1. Builder xử lý được message không có `last_data_date`. 2. `from_date` sử dụng giá trị default hợp lý (VD: epoch 0 hoặc thời điểm tạo source). 3. Không crash, không throw error. |

---

## C. CRAWLER — Thực hiện Crawl với Proxy

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PtCatPost_012 | Crawler | [High] [Positive] Crawler lấy proxy từ Proxy Manager và crawl thành công | 1. Queue `cl.pt.category_posts_crawling_requests` có request. 2. Proxy Manager có proxy active với type `PANTIP_CATEGORY_POSTS_CRAWLER`. 3. Bảng `ynm_proxies` có proxy khả dụng. | 1. Chờ Crawler consume request. 2. Kiểm tra Crawler gọi Proxy Manager lấy proxy. 3. Kiểm tra response crawl. 4. Kiểm tra queue `cl.pt.category_posts_crawled_sources`. | Proxy: `{id:'ccee378d-...', proxy:'http://thachproxy.ddns.net:12411', credential:'younet:Awq7sJXaE9xCUYWy', status:'ACTIVE'}` | 1. Crawler lấy được proxy `status=ACTIVE`. 2. Gọi Pantip API thành công (HTTP 200). 3. Response chứa `{code:200, crawlerType:'api', data:{success:true, data:[...]}}`. 4. Message crawled được publish vào `cl.pt.category_posts_crawled_sources`. |
| TC_PtCatPost_013 | Crawler | [High] [Negative] Crawler xử lý khi Pantip API trả về lỗi (HTTP 4xx/5xx) | 1. Proxy hoạt động nhưng Pantip API trả lỗi. | 1. Mock/simulate Pantip API trả HTTP 429 (Rate Limited). 2. Chờ Crawler xử lý. 3. Kiểm tra retry logic. | API Response: HTTP 429 Too Many Requests | 1. Crawler nhận được error. 2. Tăng `retries` count. 3. Nếu chưa hết retry, thử lại với proxy khác hoặc delay. 4. Log ghi nhận error code. |
| TC_PtCatPost_014 | Crawler | [High] [Negative] Crawler xử lý khi không có proxy khả dụng | 1. Tất cả proxy trong `ynm_proxies` với type `PANTIP_CATEGORY_POSTS_CRAWLER` đều `status != 'ACTIVE'`. | 1. Update tất cả proxy: `SET status='INACTIVE'`. 2. Publish request vào queue. 3. Chờ Crawler consume. | Tất cả proxy status='INACTIVE' | 1. Crawler KHÔNG thể crawl. 2. Message được nack/requeue hoặc đẩy vào dead letter queue. 3. Log ghi nhận "No available proxy". 4. Không bị crash service. |
| TC_PtCatPost_015 | Crawler | [Medium] [Edge] Crawler xử lý khi proxy bị timeout | 1. Proxy active nhưng kết nối bị timeout. | 1. Config proxy trỏ tới server không response. 2. Publish request. 3. Chờ Crawler xử lý. | Proxy: `{proxy:'http://invalid-proxy:9999', status:'ACTIVE'}`, timeout=30s | 1. Crawler gặp timeout error. 2. Retry với proxy khác (nếu có). 3. Report proxy lỗi cho Proxy Manager. 4. Không block toàn bộ pipeline. |

---

## D. RESOLVER — Xử lý Data & Mapping

### D1. Mention Mapping

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PtCatPost_016 | Resolver - Mention | [High] [Positive] Mention.id được generate đúng bằng UUID v5 từ link | 1. Queue `cl.pt.category_posts_crawled_sources` có message với data crawl thành công. 2. Resolver đang chạy. | 1. Lấy message từ `cl.pt.category_posts_crawled_sources`. 2. Kiểm tra Resolver generate UUID. 3. Kiểm tra mention output trong queue `cl.mentions_2_solr_mentions`. | Input: `topic_id=44098725` → link = `pantip.com/topic/44098725` | 1. Mention `id = 'e0d04cbe-6834-5ded-869b-1773e554896f'` (UUID v5 hash từ link `pantip.com/topic/44098725`). 2. UUID consistent — cùng topic_id luôn cho cùng UUID. |
| TC_PtCatPost_017 | Resolver - Mention | [High] [Positive] Mention.link được build đúng format | 1. Crawled data có `topic_id`. | 1. Kiểm tra mention output field `link`. | Input: `topic_id=44098725` | `link = 'pantip.com/topic/44098725'` (không có https://, đúng convention hệ thống). |
| TC_PtCatPost_018 | Resolver - Mention | [High] [Positive] Mention mapping đầy đủ các field cố định: platform, domain, country_code, mention_type | 1. Crawled data hợp lệ. | 1. Kiểm tra mention output các field cố định. | Input: bất kỳ topic Pantip hợp lệ | 1. `platform = 14`. 2. `domain = 'pantip.com'`. 3. `country_code = 'TH'`. (Need Confirm: BA spec ghi `"th"` lowercase, implementation dùng `"TH"` uppercase. Cần xác nhận convention). 4. `mention_type = 1`. 5. `mention_type_details = 1`. |
| TC_PtCatPost_019 | Resolver - Mention | [High] [Positive] Mention.identity mapping đúng từ author.id với prefix pt_ | 1. Crawled data có thông tin author. | 1. Kiểm tra field `identity` trong mention output. | Input: `author.id = 652492`, `author.name = 'double two'` | 1. `identity = 'pt_652492'` (prefix `pt_` + author.id). (Need Confirm: BA spec chỉ ghi `author.id`, nhưng implementation thêm prefix `pt_`. Theo Identity mapping thì đúng). 2. `identity_name = 'double two'`. |
| TC_PtCatPost_020 | Resolver - Mention | [High] [Positive] Mention.shard được tính đúng từ created_time (YYYYMMDD) | 1. Crawled data có `created_time`. | 1. Kiểm tra field `shard` trong mention output. | Input: `created_time = '2026-05-22T04:30:04Z'` | `shard = '20260522'` (format YYYYMMDD từ created_time, theo UTC). |
| TC_PtCatPost_021 | Resolver - Mention | [High] [Positive] Mention.id_social mapping đúng từ topic_id (string) | 1. Crawled data có `topic_id` dạng number. | 1. Kiểm tra field `id_social`. | Input: `topic_id = 44098725` (number) | `id_social = '44098725'` (string). Phải convert number → string. |
| TC_PtCatPost_022 | Resolver - Mention | [High] [Positive] Engagement metrics tính đúng khi có cả votes và comments | 1. Crawled data có `votes_count > 0` và `comments_count > 0`. | 1. Kiểm tra các field engagement trong mention output. | Input: `votes_count=3, comments_count=32, views_count=0` (topic_id=44098460) | 1. `likes = 3` (= votes_count). 2. `reaction = 3` (= votes_count). 3. `comments = 32`. 4. `shares = 0` (Pantip không có). 5. `views = 0`. 6. `engagement_total = 35` (= 3 + 32). 7. `engagement_s_c = 32` (= comments only). |
| TC_PtCatPost_023 | Resolver - Mention | [High] [Positive] Engagement metrics tính đúng khi votes=0 | 1. Crawled data có `votes_count = 0`. | 1. Kiểm tra engagement fields. | Input: `votes_count=0, comments_count=6` (topic_id=44098708) | 1. `likes = 0`, `reaction = 0`. 2. `comments = 6`. 3. `engagement_total = 6` (0 + 6). 4. `engagement_s_c = 6`. |
| TC_PtCatPost_024 | Resolver - Mention | [High] [Positive] Attachment mapping khi post có thumbnail_url (type=photo) | 1. Crawled data có `thumbnail_url`. | 1. Kiểm tra field `attachment` trong mention output. | Input: `thumbnail_url = 'https://f.ptcdn.info/311/091/000/mpfkwnzshF4SiYElXql-s.jpg'` | `attachment = '{"type":"photo","media_src":"https://f.ptcdn.info/311/091/000/mpfkwnzshF4SiYElXql-s.jpg"}'` (JSON string, type=photo, media_src=thumbnail_url). |
| TC_PtCatPost_025 | Resolver - Mention | [High] [Positive] Attachment mapping khi post KHÔNG có thumbnail_url (type=status) | 1. Crawled data KHÔNG có `thumbnail_url` (text-only post, VD: topic_type=3). | 1. Kiểm tra field `attachment`. | Input: `topic_id=44098574, topic_type=3` (không có thumbnail_url) | `attachment = '{"type":"status"}'` (JSON string, chỉ có type=status). |
| TC_PtCatPost_026 | Resolver - Mention | [High] [Positive] search_text mapping đúng: title ở index 0, body empty ở index 1 | 1. Crawled data từ category (chỉ có title, chưa có body). | 1. Kiểm tra field `search_text`. | Input: `title = 'ปูนแดงแกงร้อน ร้านอร่อย @The Glass Market บางนา'` | `search_text = ['ปูนแดงแกงร้อน ร้านอร่อย @The Glass Market บางนา', '']`. Index [0] = title, Index [1] = '' (body chưa crawl ở bước này). |
| TC_PtCatPost_027 | Resolver - Mention | [Medium] [Positive] created_date mapping đúng format ISO 8601 | 1. Crawled data có `created_time` dạng ISO string. | 1. Kiểm tra field `created_date` trong mention output. | Input: `created_time = '2026-05-22T04:30:04Z'` | `created_date = '2026-05-22T04:30:04.000Z'` (ISO 8601 với milliseconds). |
| TC_PtCatPost_028 | Resolver - Mention | [Medium] [Positive] Mention có field createdBy đúng giá trị | 1. Source message có `createdBy`. | 1. Kiểm tra field `createdBy` trong mention output. | Input: `createdBy = 'PtCategoryPostsCrawlingLoader'` | `createdBy = 'PtCategoryPostsCrawlingLoader'` — tracking đúng loader source. |
| TC_PtCatPost_029 | Resolver - Mention | [Medium] [Edge] Mention mapping khi title chứa ký tự đặc biệt (escape characters) | 1. Crawled data có title chứa `"`, `\`, `&`, emoji. | 1. Kiểm tra field `search_text` xử lý đúng ký tự đặc biệt. | Input: `title = 'ภายใต้ค่านิยมหลัก 1 ประการ \"โกงกิน ก็ยังดีกว่าสิ้นชาติ\"'` (chứa dấu `"`) | 1. `search_text[0]` chứa đầy đủ title kể cả dấu `"`. 2. Không bị truncate hay escape sai. 3. Không gây lỗi JSON parse. |
| TC_PtCatPost_030 | Resolver - Mention | [Medium] [Edge] Mention mapping khi title chứa emoji Unicode | 1. Crawled data có title chứa emoji. | 1. Kiểm tra search_text xử lý emoji. | Input: `title = '🤔อาหารอะไรที่ควรทานที่ร้านเท่านั้น...???'` (topic_id=44098507) | 1. `search_text[0] = '🤔อาหารอะไรที่ควรทานที่ร้านเท่านั้น...???'`. 2. Emoji `🤔` được giữ nguyên, không bị mất hoặc encode sai. |

### D2. Post Mapping

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PtCatPost_031 | Resolver - Post | [High] [Positive] Post mapping đầy đủ các field chính | 1. Queue `cl.pt.category_posts_crawled_sources` có crawled data. 2. Resolver đang chạy. | 1. Chờ Resolver xử lý. 2. Kiểm tra message trong queue `cl.pt.posts_2_mongo_pt_posts`. | Input: `topic_id=44098725, author.id=652492, title='ปูนแดงแกงร้อน...', created_time='2026-05-22T04:30:04Z', votes_count=0, comments_count=1` | 1. `id = 'e0d04cbe-6834-5ded-869b-1773e554896f'` (cùng UUID với Mention). 2. `id_social = '44098725'`. 3. `id_source = 'pt_652492'`. (Need Confirm: BA spec nói id_source là room ID, nhưng implementation dùng `pt_{author.id}`). 4. `title = 'ปูนแดงแกงร้อน...'`. 5. `country_code = 'TH'`. 6. `link = 'pantip.com/topic/44098725'`. |
| TC_PtCatPost_032 | Resolver - Post | [High] [Positive] Post.crawled_date được set đúng bằng timestamp hiện tại | 1. Resolver đang xử lý crawled data. | 1. Ghi nhận thời điểm Resolver xử lý. 2. Kiểm tra `crawled_date` trong post output. | Thời điểm xử lý: ~`2026-05-22T04:43:07Z` | `crawled_date = '2026-05-22T04:43:07.076Z'` (xấp xỉ thời điểm Resolver xử lý, chênh lệch < 5s). |
| TC_PtCatPost_033 | Resolver - Post | [High] [Positive] Post.likes mapping từ votes_count, Post.comments từ comments_count | 1. Crawled data có engagement metrics. | 1. Kiểm tra likes và comments trong post output. | Input: `votes_count=1, comments_count=8, views_count=0` (topic_id=44098573) | 1. `likes = 1` (= votes_count). 2. `comments = 8` (= comments_count). 3. `shares = 0` (default). 4. `views = 0` (= views_count). |
| TC_PtCatPost_034 | Resolver - Post | [High] [Positive] Post.id đồng nhất với Mention.id cho cùng topic_id | 1. Resolver đã output cả Mention và Post cho cùng topic. | 1. Lấy Mention output và Post output cho cùng topic_id. 2. So sánh field `id`. | topic_id = 44098725 | 1. Mention.id = Post.id = `'e0d04cbe-6834-5ded-869b-1773e554896f'`. 2. Cả 2 đều hash từ cùng link `pantip.com/topic/44098725`. |
| TC_PtCatPost_035 | Resolver - Post | [Medium] [Positive] Post.last_status mặc định = 0 cho post mới | 1. Topic là post mới chưa từng crawl. | 1. Kiểm tra field `last_status` trong post output. | Input: topic mới bất kỳ | `last_status = 0` (initial status cho post mới). |
| TC_PtCatPost_036 | Resolver - Post | [Medium] [Negative] Kiểm tra Post thiếu field attachment_url so với spec | 1. Crawled data có `thumbnail_url`. | 1. Kiểm tra post output có field `attachment_url` không. | Input: `thumbnail_url = 'https://f.ptcdn.info/311/091/000/mpfkwnzshF4SiYElXql-s.jpg'` | (Need Confirm: Theo BA spec, Post phải có `attachment_url = thumbnail_url`. Nhưng implementation hiện tại KHÔNG có field này trong output. Cần xác nhận với Dev/BA có cần bổ sung không). |
| TC_PtCatPost_037 | Resolver - Post | [Medium] [Negative] Kiểm tra Post thiếu field source_type so với spec | 1. Crawled data hợp lệ. | 1. Kiểm tra post output có field `source_type` không. | Input: bất kỳ topic hợp lệ | (Need Confirm: Theo BA spec, Post phải có `source_type = 1`. Nhưng implementation hiện tại KHÔNG có field này. Cần xác nhận). |

### D3. Finished Source Mapping

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PtCatPost_038 | Resolver - Finished Source | [High] [Positive] Resolver publish finished source đúng format qua exchange cl.resolved_source | 1. Resolver đã xử lý xong crawled data. | 1. Kiểm tra message trong queue `cl.news.category_links_finished_sources`. | Source: `id=439882, createdBy='PtCategoryPostsCrawlingLoader'` | 1. Finished source có `id = 439882` (= source.id). 2. `curr_page_link` = `JSON.stringify({last_data_date})`. 3. `createdBy = 'PtCategoryPostsCrawlingLoader'`. 4. `first_page_next_crawl_time` được tính từ `delay_time_rules`. |
| TC_PtCatPost_039 | Resolver - Finished Source | [High] [Positive] first_page_next_crawl_time được tính đúng theo delay_time_rules | 1. Source có `delay_time_rules` với 3 mức. | 1. Source crawl lần đầu (age < 72h). 2. Kiểm tra `first_page_next_crawl_time`. | `delay_time_rules: [{lte:72, delay:18}, {lte:144, delay:24}, {lte:999999999, delay:48}]`. Source age = 24h. | `first_page_next_crawl_time = NOW() + 18h` (vì age 24h <= 72h → delay=18h). |
| TC_PtCatPost_040 | Resolver - Finished Source | [High] [Negative] Resolver set status STOPPED khi error_code = 10000 | 1. Crawl response trả `error_code = 10000`. | 1. Simulate crawl response với `error_code=10000`. 2. Kiểm tra finished source output. | Crawl response: `{code:10000, errors:['source_invalid']}` | 1. Finished source có `status = 'STOPPED'`. 2. Source không được crawl lại tự động. 3. Cần manual intervention để reactivate. |

---

## E. SOURCE UPDATER — Update MySQL & Release Redis

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PtCatPost_041 | Source Updater | [High] [Positive] Source Updater cập nhật first_page_next_crawl_time trong MySQL | 1. Queue `cl.news.category_links_finished_sources` có finished source message. 2. Source Updater đang chạy. | 1. Chờ Source Updater consume message. 2. Query `monitor_news_categories` kiểm tra record. | Finished source: `id=439882, first_page_next_crawl_time='2026-05-22T22:43:07Z'` | 1. Record `id=439882` trong MySQL được update `first_page_next_crawl_time = '2026-05-22T22:43:07Z'`. 2. `state` chuyển về `'IDLE'` (sẵn sàng cho lần crawl tiếp). |
| TC_PtCatPost_042 | Source Updater | [High] [Positive] Source Updater release lock trong Redis | 1. Source đã lock trong Redis. Finished source message đã đến. | 1. Kiểm tra Redis key trước khi Source Updater xử lý. 2. Chờ Source Updater consume. 3. Kiểm tra Redis key sau. | Redis key trước: `lock:source:439882 = 1` | 1. Sau khi Source Updater xử lý, Redis key `lock:source:439882` đã được xóa/release. 2. Source sẵn sàng cho lần crawl tiếp. |

---

## F. DATA PUSHER — Lưu Post vào MongoDB

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PtCatPost_043 | Data Pusher | [High] [Positive] Data Pusher lưu post đúng vào MongoDB collection pt_posts | 1. Queue `cl.pt.posts_2_mongo_pt_posts` có post data. 2. Data Pusher đang chạy. 3. MongoDB khả dụng. | 1. Chờ Data Pusher consume message. 2. Query MongoDB collection `pt_posts`. | Post: `{id:'e0d04cbe-...', id_social:'44098725', title:'ปูนแดงแกงร้อน...', likes:0, comments:1}` | 1. Document tồn tại trong MongoDB `pt_posts` với `id='e0d04cbe-...'`. 2. Tất cả fields khớp: `id_social, title, likes, comments, link, created_date, crawled_date`. |
| TC_PtCatPost_044 | Data Pusher | [High] [Positive] Data Pusher xử lý upsert — không duplicate khi crawl lại cùng topic | 1. MongoDB đã có document với `id='e0d04cbe-...'` từ lần crawl trước. | 1. Crawl lại cùng topic_id=44098725. 2. Chờ Data Pusher xử lý. 3. Kiểm tra MongoDB. | topic_id=44098725, lần crawl 2 với `comments=5` (tăng từ 1) | 1. MongoDB chỉ có 1 document cho `id='e0d04cbe-...'` (không duplicate). 2. `comments` được update thành `5`. 3. `crawled_date` được update thành timestamp mới. |
| TC_PtCatPost_045 | Data Pusher | [Medium] [Negative] Data Pusher xử lý khi MongoDB connection bị mất | 1. MongoDB service bị tắt/unreachable. | 1. Stop MongoDB service. 2. Publish post message vào queue. 3. Chờ Data Pusher consume. | Post data hợp lệ, MongoDB down | 1. Data Pusher gặp connection error. 2. Message được nack/requeue (không mất data). 3. Log ghi nhận MongoDB connection error. 4. Khi MongoDB recovery, message được retry thành công. |

---

## G. END-TO-END & DATA INTEGRITY

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PtCatPost_046 | E2E Flow | [High] [Positive] Full pipeline: Loader → Builder → Crawler → Resolver → Updater → Pusher | 1. Source `id=439882` sẵn sàng (IDLE, time tới). 2. Tất cả services đang chạy. 3. Proxy available. 4. MongoDB, MySQL, Redis, Solr khả dụng. | 1. Chờ Loader chạy. 2. Monitor message qua từng queue: `crawling_sources → crawling_requests → crawled_sources → mentions_2_solr → posts_2_mongo → finished_sources`. 3. Kiểm tra MySQL, Redis, MongoDB, Solr. | Source: `id=439882, link='https://pantip.com/forum/food'` | 1. Message đi qua đúng 6 queues. 2. Mentions xuất hiện trong Solr. 3. Posts xuất hiện trong MongoDB `pt_posts`. 4. MySQL `first_page_next_crawl_time` updated. 5. Redis lock released. 6. Số mention = số post = số topic trong crawled data. |
| TC_PtCatPost_047 | Data Integrity | [High] [Positive] Số lượng Mention và Post output bằng số topic trong crawled data | 1. Crawled data trả về N topics. | 1. Đếm số topic trong crawled response. 2. Đếm số mention trong queue `cl.mentions_2_solr_mentions`. 3. Đếm số post trong queue `cl.pt.posts_2_mongo_pt_posts`. | Crawled data: 10 topics (topic_ids: 44098725, 44098708, ..., 44098460) | 1. Mention count = 10. 2. Post count = 10. 3. Mỗi topic có đúng 1 mention và 1 post (1:1:1). |
| TC_PtCatPost_048 | Data Integrity | [High] [Positive] Cross-check: Mention.id = Post.id cho mỗi topic | 1. Resolver đã output cả Mention và Post. | 1. Lấy tất cả Mention và Post output. 2. Match theo `id_social`. 3. So sánh `id` field. | 10 cặp Mention-Post | 1. Mỗi cặp có `Mention.id = Post.id`. 2. `Mention.link = Post.link`. 3. `Mention.created_date = Post.created_date`. 4. `Mention.comments = Post.comments`. 5. `Mention.likes = Post.likes`. |
| TC_PtCatPost_049 | Pagination | [High] [Positive] Resolver xử lý pagination khi crawled data có next_id | 1. Crawled response có `next_id` (không null). | 1. Kiểm tra crawled response có `next_id=44098460`. 2. Kiểm tra Resolver/Builder tạo request crawl trang tiếp theo. | Crawled response: `{success:true, data:[...10 items...], next_id:44098460, previous_id:null}` | 1. Hệ thống nhận diện còn dữ liệu (next_id != null). 2. Tạo request crawl page tiếp theo với cursor `next_id=44098460`. 3. Tiếp tục crawl cho đến khi `next_id = null` hoặc hết `from_date/to_date` range. |
| TC_PtCatPost_050 | Pagination | [Medium] [Edge] Xử lý khi crawled data có next_id = null (trang cuối) | 1. Crawled response có `next_id = null`. | 1. Kiểm tra crawled response. 2. Kiểm tra hệ thống dừng pagination. | Crawled response: `{success:true, data:[...], next_id:null}` | 1. Hệ thống dừng pagination cho source này. 2. Publish finished source. 3. Không tạo thêm request crawl mới. |
| TC_PtCatPost_051 | Data Integrity | [Medium] [Edge] Xử lý crawled data trả về empty (data: []) | 1. Crawled response thành công nhưng `data` array rỗng. | 1. Simulate crawled response có `data=[]`. 2. Kiểm tra Resolver xử lý. | Crawled response: `{success:true, data:[], next_id:null}` | 1. Resolver KHÔNG tạo Mention hay Post nào (0 records). 2. Vẫn publish finished source bình thường. 3. Không throw error. 4. Log ghi nhận "No data in response". |
| TC_PtCatPost_052 | Data Integrity | [Medium] [Edge] Xử lý khi topic không có author info (author fields null/empty) | 1. Pantip API trả topic có author.name = '' hoặc avatar empty. | 1. Simulate crawled data với author info thiếu. 2. Kiểm tra Mention và Post output. | Input: `author: {id: 7087614, name: 'สมาชิกหมายเลข 7087614', avatar: {original: '', large: 'default.png'}}` | 1. `identity = 'pt_7087614'` (vẫn có id). 2. `identity_name = 'สมาชิกหมายเลข 7087614'` (dùng tên mặc định). 3. Không crash khi avatar.original = ''. |
| TC_PtCatPost_053 | Data Integrity | [Low] [Edge] Xử lý khi có topic trùng lặp trong cùng 1 batch crawl | 1. Do pagination overlap, cùng topic xuất hiện 2 lần trong crawled data. | 1. Simulate crawled data có 2 entries cùng topic_id. 2. Kiểm tra output. | 2 entries cùng `topic_id=44098725` | 1. Chỉ tạo 1 Mention và 1 Post (deduplicate). HOẶC 2. Tạo 2 nhưng khi upsert vào DB chỉ giữ 1. 3. Không có data inconsistency. |
| TC_PtCatPost_054 | Performance | [Low] [Edge] Pipeline xử lý batch lớn (>100 topics trong 1 crawled response) | 1. Pantip API trả response có >100 topics. | 1. Simulate crawled response với 200 topics. 2. Monitor Resolver processing time. 3. Kiểm tra tất cả records được xử lý. | Crawled response: 200 topics | 1. Resolver xử lý hết 200 topics. 2. Tạo 200 mentions + 200 posts. 3. Processing time < 60s. 4. Không OOM, không timeout. |
