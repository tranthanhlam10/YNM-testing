# Test Cases v2 — Auto Token Distribution & Proxy Monthly Renew

> **Feature:** 08-F01 Token Auto-Distribution | 08-F02 Proxy Monthly Renew
> **Ngày tạo:** 21/05/2026 | **Ngày cập nhật:** 27/05/2026
> **Tài liệu tham chiếu:**
> - [08-F01 BA Spec](file:///Users/tranthanhlam/product-ai-docs/SocialHeat/specs/08-proxy-token-auto-distribution/08-F01-token-auto-distribution.md)
> - [08-F02 BA Spec](file:///Users/tranthanhlam/product-ai-docs/SocialHeat/specs/08-proxy-token-auto-distribution/08-F02-proxy-monthly-renew.md)
> - [Dev Technical Wiki](https://wiki.younetco.com/pages/viewpage.action?pageId=296354795)
> - [Dev Config Wiki](https://wiki.younetco.com/pages/viewpage.action?pageId=306840377)

---

## ⚠️ CHANGELOG SO VỚI v1 — Dựa trên Dev Config Wiki mới

> [!IMPORTANT]
> Sau khi đọc file config hướng dẫn chạy của Dev ([pageId=306840377](https://wiki.younetco.com/pages/viewpage.action?pageId=306840377)), phát hiện **8 điểm khác biệt** so với BA spec. Tất cả test cases đã được cập nhật tương ứng.

| # | Hạng mục | BA Spec viết | Dev Config thực tế | Ảnh hưởng test |
|---|----------|-------------|-------------------|----------------|
| 1 | **Tên bảng Token Config** | `crawler_config` | `crawler_token_resource_config` | ⚠️ Tất cả SQL query trong test cases phải đổi tên bảng |
| 2 | **Bảng Proxy Config** | Không đề cập | `crawler_proxy_resource_config` (bảng mới) | ✅ Thêm test cases mới cho bảng proxy config |
| 3 | **Interval Watcher** | 1 phút (60,000ms) | Default = **300,000ms (5 phút)** | ⚠️ **[Need Confirm]** BA nói 1p nhưng Dev default 5p. Cần chốt |
| 4 | **Tên platform** | Viết tắt: `FB`, `TT` | Tên đầy đủ: `facebook`, `x`, `instagram` | ⚠️ Tất cả test data phải dùng tên đầy đủ |
| 5 | **Priority field** | Bắt buộc (P0/P1/P2) | `VARCHAR(100) NULL` — nullable | ✅ Thêm edge case: priority = NULL |
| 6 | **Worker architecture** | Không mô tả | Mỗi platform tạo 1 worker riêng | ✅ Thêm test case multi-worker |
| 7 | **Deploy & Start** | Không mô tả | K8s namespace `crawler-testing`, cmd `yarn start --scope=@ynm/token-orchestrator` | ✅ Test steps chi tiết hơn với deploy info |
| 8 | **Proxy Renew** | Airflow, ngày 20 | Pipeline = `renew_proxy_pipeline`, schedule `0 0 20 * *` | ✅ Test steps ghi rõ pipeline name |

---

## THÔNG TIN MÔI TRƯỜNG TEST

| Component | Chi tiết |
|-----------|---------|
| **K8s Deployment** | https://k8s.ynm.local/#/deployment/crawler-testing/ynmpdp-5912-auto-distribution-token-testing-ynm-crawler-empty?namespace=crawler-testing |
| **Start Command** | `yarn start --scope=@ynm/token-orchestrator` |
| **Airflow (Proxy)** | http://airflow-testing.ynm.local/home → Pipeline: `renew_proxy_pipeline` |
| **DB Bảng Token Config** | `crawler_token_resource_config` |
| **DB Bảng Proxy Config** | `crawler_proxy_resource_config` |
| **Env: Platform** | `TOKEN_ORCHESTRATOR_PLATFORM` (default: `facebook`) |
| **Env: Country** | `TOKEN_ORCHESTRATOR_COUNTRY` (default: `VN`) |
| **Env: Interval** | `TOKEN_ORCHESTRATOR_INTERVAL_DISTRIBUTION` (default: `300000` ms = 5 phút) |
| **Env: Proxy API Key** | `PROXY_API_KEY` |
| **Env: Proxy Schedule** | `RENEW_PROXY_SCHEDULE` (default: `0 0 20 * *`) |

---

## MODULE 1: CRAWLER TOKEN RESOURCE CONFIG (US-05)

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_CrawlerConfig_001 | Token Resource Config | [High] [Positive] Tạo mới cấu hình luồng crawl thành công (BR-01) | 1. DB đã có bảng `crawler_token_resource_config`.\n2. Chưa có bản ghi nào cho crawler_type = 'facebook_crisis'.\n3. Service `@ynm/token-orchestrator` đang chạy trên K8s. | 1. Mở MySQL client, kết nối DB staging.\n2. Chạy lệnh INSERT tạo config mới.\n3. Chờ đúng 1 interval (theo env `TOKEN_ORCHESTRATOR_INTERVAL_DISTRIBUTION`, default 5 phút).\n4. Vào K8s Dashboard → Pods → xem log container `@ynm/token-orchestrator`.\n5. Tìm dòng log liên quan đến 'facebook_crisis'.\n6. Query lại bảng `crawler_token_resource_config` verify. | INSERT INTO crawler_token_resource_config (platform, country, priority, crawler_type, quota) VALUES ('facebook', 'VN', 'P0', 'facebook_crisis', 100); | 1. Bản ghi được tạo thành công với đầy đủ các trường: platform='facebook', country='VN', priority='P0', crawler_type='facebook_crisis', quota=100.\n2. Trường `created_at` và `updated_at` tự động set = CURRENT_TIMESTAMP.\n3. Token Orchestrator đọc được cấu hình mới từ lần chạy tiếp theo — verify qua log pod trên K8s. |
| TC_CrawlerConfig_002 | Token Resource Config | [High] [Positive] Cập nhật quota/priority, áp dụng không cần restart pod (BR-02) | 1. Bảng `crawler_token_resource_config` đã có: crawler_type='facebook_crisis', quota=100, priority='P0'.\n2. Pod `@ynm/token-orchestrator` đang chạy trên K8s. | 1. Mở MySQL client, chạy UPDATE.\n2. **KHÔNG** restart pod / KHÔNG kill pod trên K8s.\n3. Chờ interval tiếp theo (default 5 phút).\n4. Vào K8s Dashboard → Pod logs → tìm dòng log cho 'facebook_crisis'.\n5. Verify log hiển thị quota mới = 150 và priority = 'P1'. | UPDATE crawler_token_resource_config SET quota = 150, priority = 'P1' WHERE crawler_type = 'facebook_crisis';\n-- Verify:\nSELECT * FROM crawler_token_resource_config WHERE crawler_type = 'facebook_crisis'; | 1. `updated_at` tự động cập nhật (> `created_at`).\n2. Token Orchestrator áp dụng quota=150, priority='P1' từ lần chạy tiếp theo mà KHÔNG cần restart pod.\n3. Log ghi nhận giá trị mới: quota=150. |
| TC_CrawlerConfig_003 | Token Resource Config | [Medium] [Negative] Tạo trùng crawler_type + platform + country | 1. Bảng đã có: crawler_type='facebook_crisis', platform='facebook', country='VN'. | 1. Mở MySQL client.\n2. Thử INSERT bản ghi mới với cùng bộ (crawler_type, platform, country) nhưng quota khác.\n3. Quan sát kết quả trả về từ MySQL. | INSERT INTO crawler_token_resource_config (platform, country, priority, crawler_type, quota) VALUES ('facebook', 'VN', 'P1', 'facebook_crisis', 200); | 1. DB trả lỗi duplicate key / constraint violation (nếu có UNIQUE constraint).\n2. **[Need Confirm]** Nếu DB KHÔNG có constraint → bản ghi trùng được tạo → Token Orchestrator sẽ đọc config nào? Cần confirm với Dev schema có UNIQUE(crawler_type, platform, country) hay không. |
| TC_CrawlerConfig_004 | Token Resource Config | [Medium] [Negative] Tạo config thiếu trường bắt buộc (platform) | 1. Bảng `crawler_token_resource_config` tồn tại. | 1. Mở MySQL client.\n2. Thử INSERT thiếu trường `platform` (đã define NOT NULL).\n3. Quan sát kết quả. | INSERT INTO crawler_token_resource_config (country, priority, crawler_type, quota) VALUES ('VN', 'P1', 'x_news', 100); | 1. DB trả lỗi "Field 'platform' doesn't have a default value" hoặc NOT NULL constraint.\n2. Không có bản ghi nào được tạo.\n3. Tương tự cho trường `country` (NOT NULL). |
| TC_CrawlerConfig_005 | Token Resource Config | [Medium] [Edge] Tạo config với priority = NULL (schema cho phép) | 1. Bảng tồn tại.\n2. Dev schema: `priority VARCHAR(100) NULL`. | 1. INSERT bản ghi với priority = NULL.\n2. Query verify bản ghi.\n3. Chờ Token Orchestrator chạy.\n4. Kiểm tra log: crawler_type này được xử lý thế nào khi Scarcity Mode? | INSERT INTO crawler_token_resource_config (platform, country, priority, crawler_type, quota) VALUES ('facebook', 'VN', NULL, 'facebook_test', 50); | 1. Bản ghi tạo thành công (schema cho phép NULL).\n2. **[Need Confirm]** Token Orchestrator xử lý priority=NULL như thế nào? Bỏ qua? Mặc định P2? Crash? BA spec yêu cầu P0/P1/P2 nhưng DB cho phép NULL → cần confirm hành vi. |
| TC_CrawlerConfig_006 | Token Resource Config | [Medium] [Positive] Verify worker-per-platform: config chỉ ảnh hưởng đúng worker | 1. Env `TOKEN_ORCHESTRATOR_PLATFORM=facebook,x`.\n2. Pod đang chạy 2 workers (1 cho facebook, 1 cho x).\n3. Config có: facebook_crisis (platform=facebook), x_monitor (platform=x). | 1. Update quota cho 'facebook_crisis' trên DB.\n2. Chờ interval.\n3. Kiểm tra log pod — tìm dòng log của worker 'facebook'.\n4. Verify worker 'x' KHÔNG xử lý config 'facebook_crisis'. | UPDATE crawler_token_resource_config SET quota = 200 WHERE crawler_type = 'facebook_crisis';\n-- Env: TOKEN_ORCHESTRATOR_PLATFORM=facebook,x | 1. Worker 'facebook' xử lý config 'facebook_crisis' với quota mới = 200.\n2. Worker 'x' KHÔNG đọc/xử lý 'facebook_crisis' (khác platform).\n3. Log hiển thị rõ worker nào xử lý config nào. |

---

## MODULE 2: CRAWLER PROXY RESOURCE CONFIG (MỚI — từ Dev Config Wiki)

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_ProxyConfig_001 | Proxy Resource Config | [Medium] [Positive] Tạo mới proxy config thành công | 1. Bảng `crawler_proxy_resource_config` đã tồn tại trên DB. | 1. Mở MySQL client, kết nối DB staging.\n2. INSERT bản ghi proxy config mới.\n3. Query verify bản ghi vừa tạo. | INSERT INTO crawler_proxy_resource_config (country, crawler_type, quota) VALUES ('VN', 'facebook_crisis', 10);\n-- Verify:\nSELECT * FROM crawler_proxy_resource_config WHERE crawler_type = 'facebook_crisis'; | 1. Bản ghi tạo thành công: country='VN', crawler_type='facebook_crisis', quota=10.\n2. Lưu ý: bảng proxy config KHÔNG có trường `platform` (khác với bảng token config).\n3. `created_at`, `updated_at` tự động set. |
| TC_ProxyConfig_002 | Proxy Resource Config | [Low] [Edge] So sánh schema bảng proxy config vs token config | 1. Cả 2 bảng đã tồn tại. | 1. Chạy `DESCRIBE crawler_token_resource_config;`\n2. Chạy `DESCRIBE crawler_proxy_resource_config;`\n3. So sánh cấu trúc 2 bảng. | DESCRIBE crawler_token_resource_config;\nDESCRIBE crawler_proxy_resource_config; | 1. Token config có trường `platform` (NOT NULL) và `priority` (NULL).\n2. Proxy config KHÔNG có trường `platform` và KHÔNG có trường `priority`.\n3. Cả 2 đều có: id, country, crawler_type, quota, created_at, updated_at. |

---

## MODULE 3: POOL TỔNG & VÒNG ĐỜI TOKEN (BR-03, BR-04, BR-05)

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_TokenPool_001 | Pool Tổng | [High] [Positive] Import token mới vào Pool Tổng — status Active, crawler_type NULL (BR-03) | 1. Bảng `tokens` tồn tại trong DB.\n2. Biết rõ platform dùng tên đầy đủ (`facebook`, không phải `FB`). | 1. Mở MySQL client kết nối DB staging.\n2. Chạy INSERT 10 token mới.\n3. Query verify: `SELECT * FROM tokens WHERE token IN ('tk_001',...,'tk_010');`\n4. Verify tất cả token thuộc Pool Tổng: `SELECT COUNT(*) FROM tokens WHERE crawler_type IS NULL AND status='Active' AND platform='facebook' AND country='VN';` | INSERT INTO tokens (token, platform, country, status, crawler_type) VALUES\n('tk_001', 'facebook', 'VN', 'Active', NULL),\n('tk_002', 'facebook', 'VN', 'Active', NULL),\n...\n('tk_010', 'facebook', 'VN', 'Active', NULL); | 1. 10 bản ghi tạo thành công.\n2. Tất cả: status='Active', crawler_type=NULL (thuộc Pool Tổng).\n3. platform='facebook' (tên đầy đủ, KHÔNG phải 'FB'). |
| TC_TokenPool_002 | Pool Tổng | [High] [Positive] Crawler query token đúng theo crawler_type, status, platform/country (BR-05) | 1. Bảng tokens có đủ data test:\n   - 5 token: crawler_type='facebook_crisis', status='Active', platform='facebook', country='VN'\n   - 3 token: crawler_type='facebook_crisis', status='Blocked'\n   - 2 token: crawler_type=NULL (Pool Tổng) | 1. Mở MySQL client.\n2. Chạy query giống logic crawler.\n3. Đếm kết quả trả về.\n4. Verify KHÔNG có token Blocked hoặc Pool Tổng trong kết quả. | SELECT * FROM tokens WHERE crawler_type = 'facebook_crisis' AND status = 'Active' AND platform = 'facebook' AND country = 'VN';\n-- Kỳ vọng: 5 rows | 1. Query trả đúng 5 token Active.\n2. KHÔNG trả về 3 token Blocked.\n3. KHÔNG trả về 2 token Pool Tổng (crawler_type=NULL). |
| TC_TokenPool_003 | Pool Tổng | [Medium] [Positive] Token bị block → status cập nhật → Watcher bơm bù (BR-04) | 1. Token 'tk_001' có status='Active', crawler_type='facebook_crisis'.\n2. Token Orchestrator đang chạy. | 1. Mở MySQL client.\n2. UPDATE status token thành 'Blocked'.\n3. Query count token Active của 'facebook_crisis' — confirm count giảm.\n4. Chờ interval Token Orchestrator.\n5. Query lại count — verify đã bơm bù.\n6. Kiểm tra Pod log trên K8s — tìm dòng "Pumped ... tokens for facebook_crisis". | UPDATE tokens SET status = 'Blocked' WHERE token = 'tk_001';\n-- Verify count giảm:\nSELECT COUNT(*) FROM tokens WHERE crawler_type='facebook_crisis' AND status='Active'; | 1. Token 'tk_001' status='Blocked'.\n2. Count giảm 1.\n3. Token Orchestrator phát hiện count < quota → bơm 1 token bù từ Pool Tổng.\n4. Count trở lại = quota. |

---

## MODULE 4: TOKEN COUNT WATCHER / TOKEN ORCHESTRATOR (US-01, BR-06 → BR-09)

> [!WARNING]
> **Tần suất chạy:** BA spec ghi 1 phút, nhưng Dev config default `TOKEN_ORCHESTRATOR_INTERVAL_DISTRIBUTION = 300000` (5 phút). **Cần confirm với Dev** interval thực tế sẽ dùng. Tất cả test steps dưới đây ghi theo **"chờ 1 interval"** — thời gian chờ tùy thuộc config.

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_Watcher_001 | Token Orchestrator | [High] [Positive] Bơm đủ lượng thiếu khi Pool Tổng đủ token (BR-06, BR-07) | 1. Bảng `crawler_token_resource_config`: crawler_type='facebook_crisis', platform='facebook', country='VN', quota=100, priority='P0'.\n2. Bảng tokens: 80 Active với crawler_type='facebook_crisis'.\n3. Pool Tổng facebook/VN: 50 token (crawler_type=NULL).\n4. Pod `@ynm/token-orchestrator` đang chạy.\n5. Env: `TOKEN_ORCHESTRATOR_PLATFORM=facebook`, `TOKEN_ORCHESTRATOR_COUNTRY=VN`. | 1. Mở MySQL client. Chạy query đếm count trước:\n   `SELECT COUNT(*) FROM tokens WHERE crawler_type='facebook_crisis' AND status='Active' AND platform='facebook' AND country='VN';` → expect 80.\n2. Chạy query đếm Pool:\n   `SELECT COUNT(*) FROM tokens WHERE crawler_type IS NULL AND status='Active' AND platform='facebook' AND country='VN';` → expect 50.\n3. Chờ 1 interval (xem env `TOKEN_ORCHESTRATOR_INTERVAL_DISTRIBUTION`).\n4. Vào K8s Dashboard → Pod logs → tìm dòng log cho 'facebook_crisis'.\n5. Chạy lại 2 query trên. Verify count thay đổi. | Trước:\n- count(facebook_crisis) = 80\n- pool(facebook/VN) = 50\n- quota = 100\n\nSau interval:\n- count(facebook_crisis) = 100\n- pool(facebook/VN) = 30 | 1. Token Orchestrator bơm đúng 20 token (100 - 80) từ Pool Tổng.\n2. 20 token đó: crawler_type cập nhật = 'facebook_crisis'.\n3. Pool Tổng facebook/VN: 50 → 30.\n4. Log pod ghi: bơm 20 tokens cho facebook_crisis. |
| TC_Watcher_002 | Token Orchestrator | [High] [Positive] Bơm hết Pool khi Pool không đủ lượng cần (BR-08, EC-01) | 1. Config: facebook_crisis, quota=100.\n2. Tokens: 80 Active facebook_crisis.\n3. Pool Tổng facebook/VN: chỉ còn 5 token.\n4. Pod đang chạy. | 1. Query count trước: count(facebook_crisis) = 80, pool = 5.\n2. Chờ 1 interval.\n3. Kiểm tra Pod logs trên K8s — tìm dòng shortage/thiếu hụt.\n4. Query count sau: count(facebook_crisis) → expect 85.\n5. Query pool → expect 0. | Trước: count=80, pool=5, quota=100\nCần bơm: 20, Pool chỉ có: 5\nSau: count=85, pool=0 | 1. Bơm hết 5 token còn lại (tổng = 85, thiếu 15).\n2. Pool = 0.\n3. Log ghi shortage: "cần 20 nhưng Pool chỉ có 5".\n4. KHÔNG crash, KHÔNG retry. |
| TC_Watcher_003 | Token Orchestrator | [High] [Positive] Không bơm khi count đã đủ quota | 1. Config: facebook_crisis, quota=100.\n2. Tokens: đúng 100 Active facebook_crisis.\n3. Pool facebook/VN: 50 token.\n4. Pod đang chạy. | 1. Query count trước: count(facebook_crisis) = 100, pool = 50.\n2. Chờ 1 interval.\n3. Kiểm tra Pod logs — tìm dòng log "no action" hoặc skip.\n4. Query count sau — verify KHÔNG thay đổi.\n5. Query pool sau — verify KHÔNG thay đổi. | count = 100 = quota\npool = 50\n(Không thay đổi) | 1. Token Orchestrator: count(100) = quota(100) → SKIP.\n2. Pool vẫn = 50.\n3. Log: "facebook_crisis: count=100, quota=100, no action". |
| TC_Watcher_004 | Token Orchestrator | [High] [Edge] Boundary — count = quota - 1, bơm đúng 1 token (BVA) | 1. Config: facebook_crisis, quota=100.\n2. Tokens: 99 Active.\n3. Pool: ≥ 1 token. | 1. Xóa/block 1 token để count = 99.\n2. Chờ 1 interval.\n3. Query verify count = 100.\n4. Query verify pool giảm đúng 1. | count trước = 99, quota = 100\ncount sau = 100\npool giảm 1 | 1. Bơm đúng 1 token (100 - 99 = 1).\n2. Count = 100.\n3. Pool giảm đúng 1.\n4. Không bơm dư (off-by-one check). |
| TC_Watcher_005 | Token Orchestrator | [High] [Negative] Mất kết nối DB — bỏ qua, ghi log, Slack alert (BR-09, EC-04) | 1. Pod `@ynm/token-orchestrator` đang chạy.\n2. DB đang hoạt động bình thường. | 1. Simulate DB down: tắt MySQL service trên staging HOẶC block port 3306 từ pod.\n2. Chờ 1 interval.\n3. Vào K8s Dashboard → Pod logs → tìm dòng error DB connection.\n4. Kiểm tra Slack channel nhận alert.\n5. Verify pod KHÔNG crash / KHÔNG restart (check Restart Count trên K8s).\n6. Khôi phục DB connection.\n7. Chờ thêm 1 interval → verify job hoạt động lại bình thường. | DB connection: UNAVAILABLE\nPod restart count: không tăng | 1. Pod KHÔNG crash (restart count không tăng).\n2. Bỏ qua lần chạy đó.\n3. Log: "Failed to connect to DB at {timestamp}".\n4. Slack gửi EC-04 alert.\n5. Sau khi DB phục hồi → job chạy lại bình thường lần tiếp theo. |
| TC_Watcher_006 | Token Orchestrator | [High] [Positive] Bơm đúng platform — worker facebook chỉ bơm token facebook (BR-07) | 1. Env: `TOKEN_ORCHESTRATOR_PLATFORM=facebook,x`.\n2. Config: facebook_crisis (platform=facebook, quota=50, count=30).\n3. Pool: 20 token facebook/VN, 100 token x/VN. | 1. Query count trước: count(facebook_crisis) = 30.\n2. Query pool facebook/VN = 20, pool x/VN = 100.\n3. Chờ 1 interval.\n4. Query count sau: count(facebook_crisis) → expect 50.\n5. Query pool facebook/VN → expect 0.\n6. Query pool x/VN → expect **vẫn 100** (không bị ảnh hưởng). | Trước: fb_pool=20, x_pool=100\nSau: fb_pool=0, x_pool=100\nWorker facebook bơm 20 token từ fb_pool | 1. Worker 'facebook' bơm 20 token từ Pool facebook/VN.\n2. Pool x/VN KHÔNG bị ảnh hưởng (vẫn 100).\n3. Token bơm có platform='facebook', country='VN'.\n4. Log hiển thị worker 'facebook' xử lý. |
| TC_Watcher_007 | Token Orchestrator | [Medium] [Positive] Bơm cho nhiều crawler_type cùng platform trong 1 interval | 1. Env: `TOKEN_ORCHESTRATOR_PLATFORM=facebook`.\n2. Config:\n   - facebook_crisis: quota=100, count=80\n   - facebook_news: quota=50, count=40\n   - facebook_monitor: quota=30, count=30\n3. Pool facebook/VN đủ. | 1. Query count từng crawler_type trước.\n2. Chờ 1 interval.\n3. Query count từng crawler_type sau.\n4. Kiểm tra Pod logs — verify tất cả được xử lý trong cùng 1 cycle. | SELECT crawler_type, COUNT(*) as cnt\nFROM tokens\nWHERE status='Active' AND platform='facebook'\n  AND crawler_type IN ('facebook_crisis','facebook_news','facebook_monitor')\nGROUP BY crawler_type; | 1. facebook_crisis: bơm 20 (80→100).\n2. facebook_news: bơm 10 (40→50).\n3. facebook_monitor: KHÔNG bơm (30=30).\n4. Pool giảm tổng 30.\n5. Tất cả xử lý trong cùng 1 cycle. |
| TC_Watcher_008 | Token Orchestrator | [Medium] [Edge] Pool Tổng = 0, không có token để bơm | 1. Config: facebook_crisis, quota=100, count=80.\n2. Pool facebook/VN = 0. | 1. Verify pool = 0: `SELECT COUNT(*) FROM tokens WHERE crawler_type IS NULL AND status='Active' AND platform='facebook' AND country='VN';` → 0.\n2. Chờ 1 interval.\n3. Kiểm tra Pod logs → tìm shortage log.\n4. Query count → vẫn = 80. | pool = 0, count = 80, quota = 100 | 1. Bơm 0 token (Pool rỗng).\n2. Count vẫn = 80.\n3. Log ghi shortage.\n4. Trigger Pool Alert (EC-02) nếu pool < 1.5×. |
| TC_Watcher_009 | Token Orchestrator | [Medium] [Positive] Verify interval chạy đúng config TOKEN_ORCHESTRATOR_INTERVAL_DISTRIBUTION | 1. Pod đang chạy.\n2. Env `TOKEN_ORCHESTRATOR_INTERVAL_DISTRIBUTION=300000` (5 phút). | 1. Vào K8s Dashboard → Pod logs.\n2. Ghi timestamp 6 lần chạy liên tiếp (30 phút).\n3. Tính delta giữa mỗi lần.\n4. Verify delta ≈ 5 phút. | Env: TOKEN_ORCHESTRATOR_INTERVAL_DISTRIBUTION=300000\nExpected: interval ≈ 5 phút ± 30s | 1. Job chạy mỗi ~5 phút (± 30s).\n2. Trong 30 phút có ~6 lần chạy.\n3. **[Need Confirm]** BA spec ghi 1 phút. Dev config default 5 phút. Cần chốt giá trị cuối cùng. |
| TC_Watcher_010 | Token Orchestrator | [Low] [Positive] Thay đổi interval env → áp dụng ngay sau restart pod | 1. Pod đang chạy với `TOKEN_ORCHESTRATOR_INTERVAL_DISTRIBUTION=300000`.\n2. Có quyền edit deployment trên K8s. | 1. Vào K8s Dashboard → Deployment → Edit → đổi env `TOKEN_ORCHESTRATOR_INTERVAL_DISTRIBUTION=60000` (1 phút).\n2. K8s sẽ rolling restart pod.\n3. Theo dõi Pod logs 10 phút.\n4. Ghi timestamp các lần chạy.\n5. Verify interval ≈ 1 phút. | Env thay đổi: 300000 → 60000\nExpected: interval từ 5p → 1p | 1. Sau restart, job chạy mỗi ~1 phút.\n2. Env config hoạt động đúng.\n3. Không cần thay đổi code. |

---

## MODULE 5: QUEUE + TOKEN CHECKER (US-02, BR-10, BR-10b) *(Need Confirm — xem NC-1, NC-2)*

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_QueueChecker_001 | Queue + Token Checker | [High] [Positive] Slack alert khi queue tồn đọng nhưng token đủ (BR-10, EC-03) | 1. Config: facebook_crisis, quota=100.\n2. Tokens: 100 Active facebook_crisis.\n3. RabbitMQ: queue 'facebook_crisis' có > 1000 messages.\n*(Need Confirm: ngưỡng queue)*. | 1. Mở RabbitMQ Management UI → publish > 1000 messages vào queue 'facebook_crisis'.\n2. Verify token count = 100 = quota trên DB.\n3. Chờ Queue + Token Checker chạy (mỗi 5 phút).\n4. Kiểm tra Slack channel → tìm alert EC-03.\n5. Query DB → verify KHÔNG có thay đổi token.\n6. Kiểm tra RabbitMQ → queue KHÔNG bị consume bởi checker. | RabbitMQ queue 'facebook_crisis': message_count = 1500\nDB: count(facebook_crisis) = 100 = quota | 1. Slack gửi EC-03: "Luồng facebook_crisis queue tồn đọng nhưng token đủ (100/100)."\n2. KHÔNG bơm thêm token.\n3. DB không thay đổi. |
| TC_QueueChecker_002 | Queue + Token Checker | [High] [Negative] KHÔNG alert khi queue tồn đọng do thiếu token (US-02-AC-02) | 1. Config: facebook_crisis, quota=100.\n2. Tokens: 70 Active (< quota).\n3. RabbitMQ: queue tồn đọng > 1000. | 1. Setup data: count = 70 < quota = 100.\n2. Chờ Queue + Token Checker chạy.\n3. Kiểm tra Slack channel → KHÔNG có alert EC-03.\n4. Kiểm tra Pod logs → verify checker phát hiện count < quota → skip alert. | count = 70 < quota = 100\nQueue = 1500 messages | 1. KHÔNG gửi alert (count < quota → nguyên nhân tồn đọng là thiếu token → Watcher sẽ xử lý).\n2. Slack KHÔNG có message mới.\n3. Log: "facebook_crisis: count(70) < quota(100), skipping queue alert". |
| TC_QueueChecker_003 | Queue + Token Checker | [Medium] [Negative] Mất kết nối RabbitMQ (BR-10b, EC-06) | 1. Queue + Token Checker đang chạy.\n2. RabbitMQ đang hoạt động. | 1. Simulate RabbitMQ down: tắt RabbitMQ service HOẶC block AMQP port.\n2. Chờ Queue + Token Checker chạy.\n3. Kiểm tra Pod logs → tìm connection error.\n4. Kiểm tra Slack → tìm alert EC-06.\n5. Verify pod KHÔNG crash.\n6. Khôi phục RabbitMQ.\n7. Chờ lần chạy tiếp → verify hoạt động bình thường. | RabbitMQ: UNAVAILABLE | 1. Bỏ qua lần chạy.\n2. Log ghi lỗi kết nối.\n3. Slack gửi EC-06.\n4. Pod không crash.\n5. Phục hồi sau khi RabbitMQ up. |
| TC_QueueChecker_004 | Queue + Token Checker | [Medium] [Edge] crawler_type có queue nhưng không có config (EC-05) | 1. RabbitMQ có queue 'facebook_unknown'.\n2. Bảng `crawler_token_resource_config` KHÔNG có 'facebook_unknown'. | 1. Publish messages vào queue 'facebook_unknown' trên RabbitMQ Management UI.\n2. Chờ Queue + Token Checker chạy.\n3. Kiểm tra Pod logs. | RabbitMQ queue 'facebook_unknown': 500 messages\ncrawler_token_resource_config: không có 'facebook_unknown' | 1. Bỏ qua queue check cho 'facebook_unknown'.\n2. Log: "facebook_unknown not found in config, skipping".\n3. KHÔNG gửi Slack alert.\n4. Không crash. |
| TC_QueueChecker_005 | Queue + Token Checker | [Low] [Positive] Tần suất chạy đúng 5 phút (NFR) | Queue + Token Checker đang chạy. | 1. Theo dõi Pod logs 30 phút.\n2. Ghi timestamp mỗi lần checker chạy.\n3. Tính delta. | Interval: 5 phút | 1. Chạy mỗi 5 phút (± 30s).\n2. 6 lần trong 30 phút. |

---

## MODULE 6: POOL ALERT & MODE SWITCHING (US-03, BR-11, BR-12, BR-15)

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_PoolAlert_001 | Pool Alert & Mode | [High] [Positive] Slack alert khi Pool xuống Warning 1.5× (BR-11, EC-02) | 1. Config: tổng quota facebook/VN = 100.\n2. Pool Tổng facebook/VN = 160. | 1. Query tổng quota: `SELECT SUM(quota)...` → 100.\n2. Tính ngưỡng: 1.5 × 100 = 150.\n3. Xóa/block 11 token Pool Tổng → Pool còn 149.\n4. Chờ 1 interval.\n5. Kiểm tra Slack → pool_alert.\n6. Kiểm tra Pod logs → Warning mode. | Pool = 149 < 150 (Warning)\nPool = 149 > 100 (chưa Scarcity) | 1. Slack gửi EC-02 pool_alert.\n2. KHÔNG kích hoạt Scarcity Mode.\n3. Watcher vẫn bơm bình thường. |
| TC_PoolAlert_002 | Pool Alert & Mode | [High] [Edge] Boundary — Pool = 150 = 1.5× → KHÔNG alert (BVA) | Tổng quota facebook/VN = 100. Pool = 150. | 1. Setup Pool = 150 (adjust token để đạt con số chính xác).\n2. Chờ 1 interval.\n3. Kiểm tra Slack → KHÔNG có alert.\n4. Kiểm tra Pod logs → Normal Mode. | Pool = 150 = 1.5 × 100\n(Điều kiện: < 150, KHÔNG phải <=) | 1. KHÔNG gửi alert (150 KHÔNG < 150).\n2. Hệ thống ở Normal Mode. |
| TC_PoolAlert_003 | Pool Alert & Mode | [High] [Edge] Boundary — Pool = 149 → alert Warning (BVA) | Tổng quota = 100. Pool = 149. | 1. Setup Pool = 149.\n2. Chờ 1 interval.\n3. Kiểm tra Slack → CÓ alert EC-02. | Pool = 149 < 150 | 1. Slack GỬI alert EC-02.\n2. Vẫn Normal Mode (149 > 100). |
| TC_PoolAlert_004 | Pool Alert & Mode | [High] [Positive] Kích hoạt Scarcity Mode khi Pool < 1.0× (BR-12, EC-07) | Tổng quota facebook/VN = 100. Pool = 90. | 1. Setup Pool = 90.\n2. Chờ 1 interval.\n3. Kiểm tra Slack → scarcity_alert.\n4. Kiểm tra Pod logs → "Scarcity Mode ACTIVATED".\n5. Chờ thêm 1 interval → verify logic bơm theo priority. | Pool = 90 < 100 = 1.0× | 1. Scarcity Mode kích hoạt cho facebook/VN.\n2. Slack gửi scarcity_alert.\n3. Log: "Scarcity Mode ACTIVATED for facebook/VN".\n4. Từ interval tiếp: P0 full, P1 max 50%, P2 không bơm. |
| TC_PoolAlert_005 | Pool Alert & Mode | [High] [Edge] Boundary — Pool = 100 = 1.0× → KHÔNG Scarcity (BVA) | Tổng quota = 100. Pool = 100. | 1. Setup Pool = 100.\n2. Chờ 1 interval.\n3. Kiểm tra mode trong log. | Pool = 100 = 1.0 × 100 | 1. KHÔNG kích hoạt Scarcity (100 KHÔNG < 100).\n2. Ở Warning Mode (100 < 150). |
| TC_PoolAlert_006 | Pool Alert & Mode | [High] [Edge] Boundary — Pool = 99 → kích hoạt Scarcity (BVA) | Tổng quota = 100. Pool = 99. | 1. Setup Pool = 99.\n2. Chờ 1 interval.\n3. Kiểm tra Slack + Pod logs. | Pool = 99 < 100 | 1. Scarcity Mode kích hoạt.\n2. Slack gửi scarcity_alert. |

---

## MODULE 7: SCARCITY MODE — PRIORITY DISTRIBUTION (US-04, BR-13, BR-14, BR-15)

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_Scarcity_001 | Scarcity Mode | [High] [Positive] P0 bơm full quota trước (BR-13) | 1. Scarcity Mode ACTIVE cho facebook/VN.\n2. Config: facebook_crisis P0, quota=100, count=80.\n3. Pool: 50 token. | 1. Verify Scarcity Mode active trong Pod logs.\n2. Query count(facebook_crisis) = 80.\n3. Chờ 1 interval.\n4. Query count(facebook_crisis) → expect 100.\n5. Query pool → expect giảm 20. | P0: facebook_crisis, quota=100, count=80\nPool: 50 → 30 | 1. Bơm 20 token (80→100). P0 lên full quota.\n2. Pool giảm 20. |
| TC_Scarcity_002 | Scarcity Mode | [High] [Positive] P1 bơm max 50% quota (BR-13) | 1. Scarcity Mode ACTIVE.\n2. Config: facebook_news P1, quota=100, count=30.\n3. Pool: ≥ 20 token. | 1. Query count(facebook_news) = 30.\n2. Chờ 1 interval.\n3. Query count(facebook_news) → expect 50.\n4. Verify KHÔNG bơm quá 50 (= 50% × 100). | P1: facebook_news, quota=100, count=30\n50% quota = 50\nBơm 20 (30→50) | 1. Bơm 20 token (30→50 = 50% quota).\n2. KHÔNG bơm quá 50. |
| TC_Scarcity_003 | Scarcity Mode | [High] [Negative] P1 KHÔNG bơm khi count ≥ 50% (BR-13) | 1. Scarcity Mode ACTIVE.\n2. Config: facebook_news P1, quota=100, count=60. | 1. Query count = 60.\n2. Chờ 1 interval.\n3. Query count → vẫn 60.\n4. Kiểm tra log → "P1 count(60) >= 50% quota(50), skipping". | P1: count=60, 50%quota=50\n60 ≥ 50 → SKIP | 1. KHÔNG bơm (count 60 ≥ 50).\n2. KHÔNG thu hồi (P1 không bị thu hồi). |
| TC_Scarcity_004 | Scarcity Mode | [High] [Positive] P2 KHÔNG bơm, thu hồi dư về Pool (BR-14) | 1. Scarcity Mode ACTIVE.\n2. Config: facebook_monitor P2, quota=50, count=120. | 1. Query count(facebook_monitor) = 120.\n2. Chờ 1 interval.\n3. Query count → expect 50.\n4. Query pool → expect tăng 70.\n5. Verify 70 token có crawler_type = NULL. | P2: count=120, quota=50\nDư = 120-50 = 70\nThu hồi 70 về Pool | 1. Thu hồi 70 token (crawler_type → NULL).\n2. count = 50.\n3. Pool tăng 70.\n4. KHÔNG bơm cho P2. |
| TC_Scarcity_005 | Scarcity Mode | [High] [Positive] Xử lý đúng thứ tự P0→P1→P2 trong 1 cycle (BR-13) | 1. Scarcity Mode ACTIVE.\n2. 3 luồng:\n   - facebook_crisis P0: quota=100, count=80\n   - facebook_news P1: quota=100, count=30\n   - facebook_monitor P2: quota=50, count=120\n3. Pool: 40. | 1. Query count tất cả trước.\n2. Chờ 1 interval.\n3. Query count tất cả sau.\n4. Kiểm tra Pod logs → verify thứ tự xử lý.\n5. Tính toán Pool: 40 + 70(reclaim P2) - 20(bơm P0) - 20(bơm P1). | P0: 80→100 (bơm 20)\nP1: 30→50 (bơm 20)\nP2: 120→50 (thu hồi 70)\nPool: 40→70 (sau tất cả) | 1. P0 bơm 20.\n2. P1 bơm 20.\n3. P2 thu hồi 70.\n4. Log thứ tự: P0→P1→P2.\n*(Need Confirm thứ tự reclaim với Dev)* |
| TC_Scarcity_006 | Scarcity Mode | [High] [Positive] Tắt Scarcity khi Pool ≥ 1.5× (BR-15) | 1. Scarcity ACTIVE.\n2. Tổng quota = 100.\n3. Pool = 140 (< 150). | 1. Import thêm 10 token → Pool = 150.\n2. Chờ 1 interval.\n3. Kiểm tra Pod logs → "Scarcity Mode DEACTIVATED".\n4. Chờ thêm 1 interval → verify P1/P2 bơm bình thường. | Pool: 140 → 150 (≥ 1.5×100)\nScarcity OFF | 1. Tắt Scarcity Mode.\n2. Trở về Normal Mode.\n3. P1, P2 bơm bình thường từ interval tiếp. |
| TC_Scarcity_007 | Scarcity Mode | [High] [Edge] Pool = 149 → vẫn Scarcity (Anti-flapping, BR-15) | 1. Scarcity ACTIVE.\n2. Tổng quota = 100.\n3. Pool = 149. | 1. Setup Pool = 149.\n2. Chờ 1 interval.\n3. Kiểm tra mode trong log. | Pool = 149 < 150\nScarcity VẪN active | 1. Scarcity VẪN active.\n2. Hysteresis: bật ở 1.0×, tắt ở 1.5×. |
| TC_Scarcity_008 | Scarcity Mode | [Medium] [Edge] P1 count đúng 50% quota (BVA) | Scarcity ACTIVE. facebook_news P1, quota=100, count=50. | 1. Query count = 50.\n2. Chờ 1 interval.\n3. Verify count vẫn = 50. | P1: count=50 = 50%quota | 1. KHÔNG bơm (50 ≥ 50%).\n2. KHÔNG thu hồi. |
| TC_Scarcity_009 | Scarcity Mode | [Medium] [Edge] P2 count = quota → KHÔNG thu hồi | Scarcity ACTIVE. facebook_monitor P2, quota=50, count=50. | 1. Query count = 50.\n2. Chờ 1 interval.\n3. Verify count vẫn = 50. | P2: count=50 = quota | 1. KHÔNG thu hồi (không dư).\n2. KHÔNG bơm. |
| TC_Scarcity_010 | Scarcity Mode | [Medium] [Edge] P2 count < quota → KHÔNG bơm (dù thiếu) | Scarcity ACTIVE. facebook_monitor P2, quota=50, count=30. | 1. Query count = 30.\n2. Chờ 1 interval.\n3. Verify count vẫn = 30. | P2: count=30 < quota=50 | 1. KHÔNG bơm (P2 cấm bơm trong Scarcity). |
| TC_Scarcity_011 | Scarcity Mode | [Medium] [Positive] P0 không đủ Pool → bơm hết còn lại | Scarcity ACTIVE. facebook_crisis P0, quota=100, count=50. Pool=30. | 1. Query count = 50, pool = 30.\n2. Chờ 1 interval.\n3. Query count → 80.\n4. Query pool → 0. | P0: count 50→80, pool 30→0\nThiếu 20 token | 1. Bơm hết 30 (50→80, thiếu 20).\n2. Pool = 0.\n3. P1 KHÔNG bơm (Pool hết). |

---

## MODULE 8: SLACK ALERTS (Cross-cutting)

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_SlackAlert_001 | Slack Alerts | [High] [Positive] Alert latency ≤ 1 phút (NFR) | 1. Slack webhook cấu hình đúng channel.\n2. Pod đang chạy. | 1. Trigger Warning (setup Pool < 1.5×).\n2. Ghi timestamp detect event từ Pod logs.\n3. Ghi timestamp Slack message nhận được.\n4. Tính delta = Slack_ts - Log_ts. | Pool < 1.5× → trigger pool_alert | 1. Delta ≤ 60 giây.\n2. Alert không bị delay hoặc mất. |
| TC_SlackAlert_002 | Slack Alerts | [High] [Positive] Nội dung pool_alert đúng format (EC-02) | Pool < 1.5×. | 1. Trigger Warning.\n2. Đọc nội dung Slack message.\n3. Verify các trường: platform, country, pool count, quota. | Pool facebook/VN < 1.5× | 1. Nội dung chứa platform='facebook', country='VN'.\n2. Có thông tin pool count và ngưỡng.\n3. **Lưu ý:** Dùng 'facebook' (không phải 'FB'). |
| TC_SlackAlert_003 | Slack Alerts | [High] [Positive] Nội dung queue_alert đúng format (EC-03) | Queue tồn đọng, token đủ quota. | 1. Trigger queue alert.\n2. Đọc nội dung Slack message.\n3. Verify các trường: crawler_type, count, quota. | Queue 'facebook_crisis' backlog\ncount=100, quota=100 | 1. Nội dung chứa crawler_type='facebook_crisis'.\n2. Có count/quota: (100/100). |

---

## MODULE 9: PROXY MONTHLY RENEW (08-F02)

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_ProxyRenew_001 | Proxy Monthly Renew | [High] [Positive] Sync thành công ngày 20 (BR-01, BR-02) | 1. Ngày 20.\n2. Env `PROXY_API_KEY` đã set đúng.\n3. Airflow pipeline `renew_proxy_pipeline` đã deploy.\n4. Bảng proxies: 50 proxy cũ Active.\n5. Bảng `crawler_proxy_resource_config` đã có config. | 1. Mở Airflow UI: http://airflow-testing.ynm.local/home\n2. Tìm DAG `renew_proxy_pipeline`.\n3. Trigger DAG manually (hoặc chờ schedule `0 0 20 * *`).\n4. Theo dõi DAG Run status trên Airflow UI → chờ SUCCESS.\n5. Kiểm tra Airflow task logs → tìm API call BuyProxies.\n6. Mở MySQL client → query bảng proxies verify proxy mới.\n7. Query verify proxy cũ status = 'Expired'.\n8. Kiểm tra Slack channel. | Airflow: trigger DAG `renew_proxy_pipeline`\nAPI response: 60 proxy mới\nBảng proxies trước: 50 Active | 1. DAG chạy thành công (status = SUCCESS trên Airflow).\n2. 60 proxy mới import: status='Active'.\n3. 50 proxy cũ: status='Expired'.\n4. Thứ tự: import trước → vô hiệu hóa sau.\n5. Slack thông báo thành công. |
| TC_ProxyRenew_002 | Proxy Monthly Renew | [High] [Negative] API thất bại lần 1, retry lần 2 thành công (BR-03) | 1. Ngày 20.\n2. Airflow DAG `renew_proxy_pipeline` deploy.\n3. API BuyProxies sẽ lỗi lần 1, thành công lần 2 (mock). | 1. Trigger DAG `renew_proxy_pipeline` trên Airflow UI.\n2. Theo dõi task logs → lần 1 FAIL.\n3. Chờ retry (5 phút theo config).\n4. Task logs → lần 2 SUCCESS.\n5. Query DB verify proxy mới import.\n6. Verify proxy cũ vẫn Active trong thời gian chờ retry. | Lần 1: API 500/timeout\nLần 2: API 200 + 60 proxy\nRetry interval: 5 phút | 1. Lần 1: log lỗi, DB KHÔNG thay đổi.\n2. Chờ 5 phút → retry.\n3. Lần 2: import + vô hiệu hóa OK.\n4. Proxy cũ hoạt động suốt thời gian retry. |
| TC_ProxyRenew_003 | Proxy Monthly Renew | [High] [Negative] API thất bại 3 lần → giữ proxy cũ, Slack alert (BR-04, EC-01) | 1. Ngày 20.\n2. API BuyProxies luôn lỗi (mock). | 1. Trigger DAG trên Airflow.\n2. Theo dõi task logs → 3 lần FAIL liên tiếp.\n3. DAG Run status → FAILED.\n4. Query DB → verify proxy cũ KHÔNG thay đổi.\n5. Kiểm tra Slack → alert EC-01. | API: luôn 500\nRetry: 3 × 5 phút = 15 phút | 1. Sau 3 retry → DB KHÔNG thay đổi.\n2. Proxy cũ vẫn Active.\n3. Slack: "[Alert] Không thể đồng bộ proxy."\n4. DAG status = FAILED trên Airflow. |
| TC_ProxyRenew_004 | Proxy Monthly Renew | [Medium] [Negative] Job KHÔNG chạy ngày khác (ngày 19, 21) | 1. Airflow DAG schedule: `0 0 20 * *`.\n2. Ngày hiện tại = 19 hoặc 21. | 1. Mở Airflow UI → DAG `renew_proxy_pipeline`.\n2. Kiểm tra DAG Runs → KHÔNG có run ngày 19.\n3. Kiểm tra Scheduled → next run = ngày 20.\n4. Verify KHÔNG có task logs ngày 19/21. | Schedule: 0 0 20 * *\nNgày test: 19 và 21 | 1. KHÔNG trigger ngày 19 hay 21.\n2. Airflow schedule hiển thị next run = ngày 20.\n3. DB không thay đổi. |
| TC_ProxyRenew_005 | Proxy Monthly Renew | [Medium] [Positive] Import trước → Vô hiệu hóa sau (zero downtime) | 1. Ngày 20. API OK.\n2. 50 proxy cũ Active. | 1. Trigger DAG.\n2. Theo dõi task logs **chi tiết theo timestamp**.\n3. Nếu có thể: query DB giữa bước import và bước expire:\n   `SELECT COUNT(*) FROM proxies WHERE status='Active';`\n   → expect > 50 (cũ + mới cùng Active). | Log timestamps: import_at < expire_at | 1. Bước 1: Import 60 mới → DB có 110 Active.\n2. Bước 2: Expire 50 cũ → DB còn 60 Active.\n3. Tại mọi thời điểm: ≥ 50 proxy Active (zero downtime). |
| TC_ProxyRenew_006 | Proxy Monthly Renew | [Low] [Edge] API timeout đúng config (NFR) | Ngày 20. API chậm > timeout. | 1. Mock API delay > timeout.\n2. Trigger DAG.\n3. Kiểm tra task logs → timeout error.\n4. Verify retry triggered. | API delay > timeout config | 1. API bị timeout.\n2. Log ghi error.\n3. Trigger retry. |
| TC_ProxyRenew_007 | Proxy Monthly Renew | [Low] [Edge] Retry interval đúng 5 phút (NFR) | API lỗi liên tiếp. | 1. Mock API lỗi 3 lần.\n2. Ghi timestamp mỗi retry từ Airflow task logs.\n3. Tính delta. | Retry 1→2→3\nExpected: 5 phút ± 30s | 1. Delta giữa mỗi retry ≈ 5 phút.\n2. Tổng ≈ 10-15 phút. |

---

## MODULE 10: DATA INTEGRITY & REGRESSION

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_Regression_001 | Regression | [High] [Positive] Crawler hiện tại không bị ảnh hưởng sau deploy | 1. Code mới deploy trên K8s staging.\n2. Crawler đang chạy. | 1. Vào K8s Dashboard → verify deployment `ynmpdp-5912-auto-distribution-token-testing-ynm-crawler-empty` đã deploy thành công (namespace: crawler-testing).\n2. Chạy 1 luồng crawl facebook_crisis.\n3. Kiểm tra crawler lấy được token → crawl bình thường.\n4. Kiểm tra Pod logs crawler → không có lỗi mới. | Deployment: crawler-testing namespace\nCrawler: facebook_crisis | 1. Crawler vẫn query token đúng (BR-05).\n2. Không lỗi mới.\n3. Data không gián đoạn. |
| TC_Regression_002 | Regression | [High] [Positive] Token Pool migration đúng cấu trúc | DB đã migrate. | 1. Query: `SELECT COUNT(*) FROM tokens WHERE status IS NULL;` → expect 0.\n2. Query: `SELECT COUNT(*) FROM tokens WHERE crawler_type IS NULL AND status='Active';` → Pool Tổng.\n3. Verify mỗi token có đủ: token, platform, country, status, crawler_type. | platform dùng tên đầy đủ: 'facebook', 'x', 'instagram' (KHÔNG phải 'FB', 'TT') | 1. Không token thiếu status.\n2. Token chưa gán: crawler_type=NULL.\n3. Platform dùng tên đầy đủ (theo Dev config). |
| TC_DataIntegrity_001 | Data Integrity | [High] [Positive] Tổng token không thay đổi sau bơm/thu hồi | Hệ thống đã chạy nhiều cycle. | 1. Query tổng token Active TRƯỚC:\n   `SELECT COUNT(*) FROM tokens WHERE status='Active';` → ghi lại = X.\n2. Chạy nhiều cycle (bơm Normal + Scarcity + thu hồi).\n3. Query tổng token Active SAU → expect = X.\n4. Verify: `SELECT COUNT(*) FROM tokens WHERE status='Active';` = X. | Total trước = Total sau = X | 1. Tổng KHÔNG đổi (token chỉ chuyển crawler_type, không tạo/xóa).\n2. Không token "mất". |
| TC_DataIntegrity_002 | Data Integrity | [Medium] [Positive] Config đầy đủ cho tất cả crawler | 1. Bảng `crawler_token_resource_config` đã seed.\n2. Bảng tokens có data. | 1. Query crawler_type từ tokens:\n   `SELECT DISTINCT crawler_type FROM tokens WHERE crawler_type IS NOT NULL;`\n2. Query crawler_type từ config:\n   `SELECT crawler_type FROM crawler_token_resource_config;`\n3. Cross-check: mọi crawler_type có token đều có config. | So sánh 2 list crawler_type | 1. Mọi crawler_type trong tokens đều có bản ghi trong config.\n2. Mỗi bản ghi config có đủ: crawler_type, platform, country, quota.\n3. **Lưu ý:** priority có thể NULL (schema cho phép). |

---

## TỔNG KẾT v2

| Module | Số TC v1 | Số TC v2 | Thay đổi |
|--------|----------|----------|----------|
| Token Resource Config (US-05) | 4 | **6** | +2 (priority NULL, worker-per-platform) |
| Proxy Resource Config (MỚI) | 0 | **2** | +2 (bảng mới từ Dev config) |
| Pool Tổng & Token Lifecycle | 3 | 3 | Đổi tên platform/bảng |
| Token Orchestrator (US-01) | 9 | **10** | +1 (thay đổi interval env) |
| Queue + Token Checker (US-02) | 5 | 5 | Đổi tên bảng/platform |
| Pool Alert & Mode Switching (US-03) | 6 | 6 | Đổi tên platform |
| Scarcity Mode (US-04) | 11 | 11 | Đổi tên platform |
| Slack Alerts | 3 | 3 | Đổi tên platform |
| Proxy Monthly Renew (F02) | 7 | 7 | Thêm chi tiết Airflow |
| Data Integrity & Regression | 4 | 4 | Đổi tên bảng/platform |
| **TỔNG** | **52** | **57** | **+5 test cases mới** |

### Tổng hợp [Need Confirm] từ Dev Config Wiki

| # | Câu hỏi | Ảnh hưởng |
|---|---------|-----------|
| NC-7 | **Interval mặc định = 5 phút (300000ms)**. BA spec ghi 1 phút. Giá trị cuối cùng sẽ dùng cho production? | Ảnh hưởng TC_Watcher_009, TC_Watcher_010, toàn bộ thời gian chờ test |
| NC-8 | **priority = NULL**: Token Orchestrator xử lý thế nào khi crawler_type có priority NULL? Mặc định P2? Bỏ qua? | Ảnh hưởng TC_CrawlerConfig_005, logic Scarcity Mode |
| NC-9 | **UNIQUE constraint**: Bảng `crawler_token_resource_config` có UNIQUE(crawler_type, platform, country)? | Ảnh hưởng TC_CrawlerConfig_003, data integrity |
| NC-10 | **Bảng proxy config**: `crawler_proxy_resource_config` được sử dụng bởi job nào? Token Orchestrator hay Proxy Renew pipeline? | Ảnh hưởng TC_ProxyConfig_001 |
