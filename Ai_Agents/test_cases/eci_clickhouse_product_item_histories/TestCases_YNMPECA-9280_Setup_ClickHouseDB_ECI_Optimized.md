# TEST CASES OPTIMIZED
## YNMPECA-9280 - Setup ClickHouseDB VN và TL cho ECI

| Field | Value |
|---|---|
| Jira chính | https://jira.younetco.com/browse/YNMPECA-9280 |
| Test Plan | `Ai_Agents/test_plans/local/eci_clickhouse_product_item_histories/TestPlan_YNMPECA-9280_Setup_ClickHouseDB_ECI.md` |
| Test cases gốc | `Ai_Agents/test_cases/eci_clickhouse_product_item_histories/TestCases_YNMPECA-9280_Setup_ClickHouseDB_ECI.md` |
| Phiên bản | v1.1 - Optimized |
| Ngày tạo | 06/07/2026 |
| Ghi chú | Rút gọn từ 85 xuống 59 test cases, bỏ các case quá nhỏ hoặc trùng mục tiêu nhưng vẫn giữ coverage cao cho schema, adapter, Merge, query, parity, pusher, realtime, App, rollback và concurrent behavior. |

---

| TEST CASE ID | MODULE/FEATURE | TEST NAME | PRE-CONDITION | TEST STEPS | TEST DATA | EXPECTED RESULT |
|---|---|---|---|---|---|---|
| TC_READY_001 | Readiness / Need Confirm | [High][Positive] Chốt cơ chế Merge/FINAL trước khi execute calculation | Có Dev/DBA tham gia NC meeting; QA có test plan và review file | 1. Hỏi Dev cơ chế xử lý duplicate trước calculation.<br>2. Ghi nhận dùng `OPTIMIZE ... FINAL`, query `FINAL`, trigger Merge job, hoặc cơ chế khác.<br>3. Cập nhật expected cho các case dedup/calculation. | NC-2; ví dụ duplicate cùng `(product_item_id, crawled_date)` nhưng khác `updated_date` | Cơ chế Merge/FINAL được chốt rõ. Nếu chưa chốt, block các case dedup, `checkInvalidRecords`, `calculatePIHistories` và concurrent calculation. |
| TC_READY_002 | Readiness / Need Confirm | [High][Boundary] Chốt rule biên ngày cho `checkInvalidRecords` và `calculatePIHistories` | Có Dev/BA tham gia NC meeting | 1. Đưa ví dụ record đúng bằng `endDate`.<br>2. Confirm `checkInvalidRecords` dùng `< endDate`.<br>3. Confirm `calculatePIHistories` dùng `<= endDate`. | `{"startDate":"2026-07-06 00:00:00","endDate":"2026-07-07 00:00:00","recordAtEndDate":"2026-07-07 00:00:00"}` | QA có rule pass/fail chính thức cho boundary date. Nếu chưa chốt, block toàn bộ case biên ngày. |
| TC_READY_003 | Readiness / Need Confirm | [High][Boundary] Chốt timezone VN/TL cho `crawled_date` và partition tuần | Có Dev/BA tham gia NC meeting | 1. Confirm input date dùng UTC hay `Asia/Ho_Chi_Minh`.<br>2. Confirm TL có timezone/rule riêng không.<br>3. Chốt cách convert khi query ClickHouse và Timescale. | `{"vnDate":"2026-07-06 00:00:00","tlDate":"2026-07-06 00:00:00","partition":"toYYYYMMDD(toMonday(crawled_date))"}` | Không còn mơ hồ timezone. QA có thể xác định đúng ngày, tuần, partition và expected result cho VN/TL. |
| TC_READY_004 | Readiness / Need Confirm | [High][Positive] Chốt parity threshold giữa Timescale và ClickHouse | Có PM/BA/Dev tham gia NC meeting | 1. Hỏi threshold cho count, sold, GMV, average price, discount.<br>2. Hỏi tolerance rounding/decimal.<br>3. Ghi threshold vào sheet execution. | `{"count":"100% expected","decimalTolerance":"Need Confirm, đề xuất <0.01%"}` | Có tiêu chí pass/fail rõ. Sai lệch vượt threshold phải log bug hoặc block sign-off. |
| TC_READY_005 | Readiness / Need Confirm | [High][Positive] Xác nhận data Q1/2026+ đã load vào ClickHouse | QA có quyền query ClickHouse | 1. Query min/max `crawled_date`.<br>2. Count data theo tháng từ 01/2026.<br>3. So sánh với thông tin Dev/DBA bàn giao. | `{"from":"2026-01-01","to":"current_date","table":"eci.product_item_histories"}` | ClickHouse có data đủ dài để test parity và App 1 năm. Nếu chưa có, block parity dài hạn và App adapter 1 năm. |
| TC_READY_006 | Readiness / Need Confirm | [Medium][Positive] Chốt queue/topic và ack/retry của pusher raw histories | Sub-task `YNMPECA-9286` sẵn sàng hoặc Dev confirm design | 1. Hỏi queue/topic input của raw histories pusher.<br>2. Hỏi cơ chế ack khi insert ClickHouse fail.<br>3. Hỏi retry/dead-letter policy. | `{"subTask":"YNMPECA-9286","failure":"ClickHouse insert timeout"}` | QA có đủ thông tin để test pusher failure. Nếu chưa chốt, các case raw pusher failure ở trạng thái Blocked. |
| TC_CH_SCHEMA_001 | ClickHouse Schema | [High][Positive] Kiểm tra database và bảng ClickHouse chính tồn tại | QA có quyền query ClickHouse Testing/Staging | 1. Chạy `SHOW DATABASES`.<br>2. Chạy `SHOW TABLES FROM eci`.<br>3. Kiểm tra local và distributed table. | `{"database":"eci","localTable":"product_item_histories_local","distributedTable":"product_item_histories"}` | Database `eci`, bảng `product_item_histories_local` và `product_item_histories` tồn tại đúng tên, đúng môi trường. |
| TC_CH_SCHEMA_002 | ClickHouse Schema | [High][Positive] Kiểm tra schema field và data type theo Tech Wiki | Bảng ClickHouse đã tạo | 1. Chạy `DESCRIBE TABLE eci.product_item_histories_local`.<br>2. Đối chiếu nhóm field time, product, shop, price, sales, flags, ratings, brand.<br>3. Ghi nhận field thiếu/sai type. | Tech Wiki pageId `308413227`; field mẫu: `crawled_date`, `product_item_id`, `total_sold`, `price`, `sell_price`, `is_abnormal` | Các field bắt buộc tồn tại đúng type/nullable. Sai field hoặc sai type ảnh hưởng insert/query phải log bug High. |
| TC_CH_SCHEMA_003 | ClickHouse Storage | [High][Positive] Kiểm tra engine, version column và ORDER BY | QA có quyền chạy `SHOW CREATE TABLE` | 1. Chạy `SHOW CREATE TABLE eci.product_item_histories_local`.<br>2. Kiểm tra engine.<br>3. Kiểm tra version column và ORDER BY. | `{"engine":"ReplicatedReplacingMergeTree","versionColumn":"updated_date","orderBy":"(product_item_id, crawled_date)"}` | Engine, version column và ORDER BY đúng Tech Wiki. Nếu version column không phải `updated_date`, block dedup test. |
| TC_CH_SCHEMA_004 | ClickHouse Storage | [High][Positive] Kiểm tra partition tuần và distributed sharding key | QA có quyền xem DDL | 1. Kiểm tra `PARTITION BY toYYYYMMDD(toMonday(crawled_date))`.<br>2. Kiểm tra Distributed engine dùng `cityHash64(product_item_id)`.<br>3. Query thử một PI qua distributed table. | `{"partition":"weekly by Monday","sharding":"cityHash64(product_item_id)"}` | Partition theo tuần đúng; distributed table routing theo `product_item_id`, không thiếu dữ liệu khi query distributed table. |
| TC_CH_SCHEMA_005 | Materialized Fields | [High][Positive] Verify `delta_sold` và `gmv` tự tính đúng khi sold tăng | QA có quyền insert test data hoặc Dev hỗ trợ insert | 1. Insert record test.<br>2. Query lại record theo `product_item_id`.<br>3. Kiểm tra `delta_sold` và `gmv`. | `{"product_item_id":"test_ch_sold_up_001","total_sold":120,"last_total_sold":100,"sell_price":15000}` | `delta_sold=20`, `gmv=300000`; insert không lỗi kiểu dữ liệu. |
| TC_CH_SCHEMA_006 | Materialized Fields | [High][Edge] Verify null và price=0 không làm sai `delta_sold`, `gmv`, discount | Có data test null/zero | 1. Insert record thiếu `total_sold`.<br>2. Insert record thiếu `last_total_sold`.<br>3. Insert record `price=0`, `sell_price=0`.<br>4. Query output. | `{"cases":[{"total_sold":null,"last_total_sold":100},{"total_sold":100,"last_total_sold":null},{"price":0,"sell_price":0}]}` | `delta_sold=0` khi thiếu sold; `gmv=0` khi không đủ điều kiện; discount không chia cho 0 và không làm job crash. |
| TC_CH_SCHEMA_007 | Dedup / Merge | [High][Positive] Verify duplicate cùng key giữ row có `updated_date` mới hơn | Cơ chế Merge/FINAL đã chốt ở `TC_READY_001` | 1. Insert hai row cùng `(product_item_id, crawled_date)` nhưng khác `updated_date` và `total_sold`.<br>2. Trigger Merge hoặc query `FINAL` theo design.<br>3. Query record final. | `{"product_item_id":"test_ch_dup_001","crawled_date":"2026-07-06 10:00:00","old":{"updated_date":"10:00","total_sold":120},"new":{"updated_date":"10:05","total_sold":130}}` | Row có `updated_date` mới hơn được chọn. Calculation không cộng cả hai row duplicate. |
| TC_CH_SCHEMA_008 | ClickHouse Storage | [Medium][Boundary] Verify record đầu tuần/cuối tuần vào đúng partition | Có quyền query `system.parts` | 1. Insert hoặc chọn record thứ Hai và Chủ nhật.<br>2. Query partition qua `system.parts`.<br>3. So sánh với `toMonday(crawled_date)`. | `{"monday":"2026-07-06 00:00:00","sunday":"2026-07-12 23:59:59"}` | Record được map đúng partition tuần, không lệch do timezone hoặc date conversion. |
| TC_ADAPTER_001 | ClickhouseAdapter | [High][Positive] Adapter `ynm-eca` kết nối ClickHouse thành công | `ynm-eca` đã deploy config ClickHouse | 1. Trigger health check hoặc query đơn giản qua adapter.<br>2. Xem log kết nối.<br>3. Kiểm tra lỗi auth/timeout. | `{"service":"ynm-eca","database":"eci","query":"SELECT 1"}` | Adapter kết nối đúng host/database/user; không lỗi authentication; log không lộ secret. |
| TC_ADAPTER_002 | ClickhouseAdapter | [High][Positive] Adapter bind đúng date, cursor và limit | Có nhiều PI test trong ClickHouse | 1. Chạy query có `startDate`, `endDate`, `latestProductItemId`, `limit`.<br>2. Kiểm tra output lần 1.<br>3. Lấy item cuối làm cursor và chạy lần 2. | `{"startDate":"2026-07-06","endDate":"2026-07-13","latestProductItemId":"test_pi_010","limit":500}` | Date được parse đúng; cursor chỉ lấy PI lớn hơn `latestProductItemId`; limit đúng; không lặp dữ liệu giữa các batch. |
| TC_ADAPTER_003 | ClickhouseAdapter | [High][Negative] ClickHouse timeout hoặc query error không làm service crash | Có thể simulate timeout/query lỗi trên test env | 1. Nhờ Dev simulate timeout hoặc syntax error có kiểm soát.<br>2. Trigger job qua adapter.<br>3. Kiểm tra service status và log. | `{"simulate":"query_timeout_or_syntax_error"}` | Service không crash; job fail/retry theo design; log có module, query/job id, error message; không lộ password/token. |
| TC_ADAPTER_004 | Feature Flag / Config | [Medium][Conditional] Flag OFF dùng Timescale, flag ON dùng ClickHouse | Dev confirm có feature flag/config datasource | 1. Set flag OFF và chạy query mẫu.<br>2. Kiểm tra log datasource là Timescale.<br>3. Set flag ON và chạy lại.<br>4. Kiểm tra log datasource là ClickHouse. | `{"flag":"USE_CLICKHOUSE_PRODUCT_ITEM_HISTORIES","off":"Timescale","on":"ClickHouse"}` | Datasource đổi đúng theo flag; output vẫn đúng expected; nếu không có flag thì ghi Need Confirm và chuyển thành test rollback config. |
| TC_CHKINV_001 | checkInvalidRecords | [High][Positive] Trả đúng PI có `delta_sold < 0` và `is_abnormal = 0` | Có dataset sold giảm trong ClickHouse | 1. Chuẩn bị PI sold giảm.<br>2. Chạy `checkInvalidRecords`.<br>3. Kiểm tra list `product_item_id`. | `{"product_item_id":"test_invalid_001","total_sold":90,"last_total_sold":100,"is_abnormal":0}` | Output có `test_invalid_001`; chỉ trả PI có `delta_sold < 0` và `is_abnormal = 0`. |
| TC_CHKINV_002 | checkInvalidRecords | [High][Negative] Không trả PI sold tăng, sold bằng nhau hoặc abnormal | Có dataset mixed | 1. Chuẩn bị PI sold tăng, sold bằng nhau, sold giảm nhưng abnormal.<br>2. Chạy `checkInvalidRecords`.<br>3. Kiểm tra output. | `{"cases":[{"id":"sold_up","delta_sold":20},{"id":"sold_same","delta_sold":0},{"id":"abnormal_down","delta_sold":-10,"is_abnormal":1}]}` | Output không chứa các PI không hợp lệ; không false positive. |
| TC_CHKINV_003 | checkInvalidRecords | [High][Boundary] Date range dùng `>= startDate` và `< endDate` | Rule date đã chốt | 1. Tạo record tại `startDate`.<br>2. Tạo record đúng bằng `endDate`.<br>3. Chạy query.<br>4. Kiểm tra output. | `{"startDate":"2026-07-06 00:00:00","endDate":"2026-07-13 00:00:00","recordAtStart":"included","recordAtEnd":"excluded"}` | Record tại `startDate` được tính; record đúng `endDate` không được trả về nếu rule là `< endDate`. |
| TC_CHKINV_004 | checkInvalidRecords | [High][Positive] Cursor, sort ASC và limit hoạt động đúng | Có hơn 500 PI sold giảm | 1. Chạy query với cursor rỗng và `limit=500`.<br>2. Kiểm tra count/order.<br>3. Chạy tiếp với cursor là item cuối batch trước. | `{"limit":500,"latestProductItemId":"last_id_from_previous_batch"}` | Output tối đa 500 PI, sort `product_item_id ASC`, batch tiếp theo không lặp PI cũ. |
| TC_CHKINV_005 | checkInvalidRecords | [High][Edge] Query không sai khi có duplicate row trước calculation | Cơ chế Merge/FINAL đã chốt | 1. Chuẩn bị duplicate cùng PI/time.<br>2. Trigger Merge/FINAL theo design.<br>3. Chạy `checkInvalidRecords`. | `{"product_item_id":"test_dup_negative_001","old_delta":-10,"new_delta":20}` | Output dựa trên row thắng sau Merge/FINAL, không trả PI sai do row cũ duplicate. |
| TC_CHKINV_006 | checkInvalidRecords | [Medium][Performance] Batch 500 chạy trong threshold đã chốt | Có data Q1/2026+ và threshold đã chốt | 1. Chạy query trên dataset dài.<br>2. Đo duration.<br>3. Kiểm tra log timeout/resource. | `{"range":"2026-Q1+","limit":500,"threshold":"Need Confirm, đề xuất <60s"}` | Query hoàn tất trong threshold; không timeout; nếu vượt threshold thì log bug performance hoặc Need Confirm. |
| TC_CHKINV_007 | checkInvalidRecords | [Medium][Compare] `DISTINCT` và `GROUP BY` trả cùng tập PI nếu Dev đổi query | Dev confirm có đổi query hoặc muốn benchmark | 1. Chạy query theo logic `DISTINCT`.<br>2. Chạy query theo logic `GROUP BY`.<br>3. So sánh output và duration. | `{"dataset":"sold_down_with_duplicate","limit":500}` | Hai query trả cùng tập `product_item_id`; nếu khác output, block đổi query hoặc log bug. |
| TC_CALC_001 | calculatePIHistories | [High][Positive] Tính đúng sold và GMV cho PI có sold tăng | Có data sold tăng trong ClickHouse | 1. Chuẩn bị PI có `delta_sold > 0`.<br>2. Chạy `calculatePIHistories`.<br>3. Kiểm tra `sold` và `gmv`. | `{"product_item_id":"test_calc_up_001","total_sold":120,"last_total_sold":100,"sell_price":15000}` | `sold=20`, `gmv=300000`; output có đúng `product_item_id`. |
| TC_CALC_002 | calculatePIHistories | [High][Negative] Không tính record `delta_sold <= 0` hoặc abnormal | Có dataset mixed | 1. Chuẩn bị sold giảm, sold bằng nhau, sold tăng abnormal.<br>2. Chạy calculation.<br>3. Kiểm tra aggregate. | `{"cases":[{"delta_sold":-10},{"delta_sold":0},{"delta_sold":20,"is_abnormal":1}]}` | Các record không hợp lệ không đóng góp vào `sold`, `gmv`, average và abnormal output. |
| TC_CALC_003 | calculatePIHistories | [High][Positive] Tính đúng avg price, avg sell price và discount percent | Có nhiều record cùng PI với giá khác nhau | 1. Chuẩn bị 3 record cùng PI.<br>2. Chạy calculation.<br>3. Tính expected bằng tay và so sánh. | `{"product_item_id":"test_calc_avg_001","records":[{"price":20000,"sell_price":15000},{"price":30000,"sell_price":24000},{"price":10000,"sell_price":8000}]}` | `avg_price`, `avg_sell_price`, `avg_percent_discount` đúng theo query ClickHouse; sai lệch chỉ được chấp nhận trong tolerance đã chốt. |
| TC_CALC_004 | calculatePIHistories | [High][Edge] Null price, null sell price và price=0 không làm job lỗi | Có dataset null/zero price | 1. Chuẩn bị record `sell_price=null`.<br>2. Chuẩn bị record `price=0`.<br>3. Chạy calculation.<br>4. Kiểm tra output và log. | `{"cases":[{"price":20000,"sell_price":null},{"price":0,"sell_price":0}]}` | Job không crash; `gmv=0` khi thiếu sell price; discount percent không chia cho 0 và được xử lý `NULL`. |
| TC_CALC_005 | calculatePIHistories | [High][Boundary] Date range dùng `>= startDate` và `<= endDate` | Rule date đã chốt | 1. Tạo record tại `startDate`.<br>2. Tạo record đúng bằng `endDate`.<br>3. Chạy calculation.<br>4. Kiểm tra output. | `{"startDate":"2026-07-06 00:00:00","endDate":"2026-07-13 00:00:00","recordAtEnd":"included"}` | Record tại `startDate` và đúng `endDate` được tính nếu rule là `<= endDate`. |
| TC_CALC_006 | calculatePIHistories | [High][Positive] Cursor, order và limit 1000 hoạt động đúng | Có hơn 1000 PI hợp lệ | 1. Chạy calculation với `limit=1000`.<br>2. Kiểm tra order ASC.<br>3. Chạy batch tiếp theo bằng cursor item cuối batch trước. | `{"limit":1000,"latestProductItemId":"last_id_from_previous_batch"}` | Output tối đa 1000 PI, sort ASC, không lặp PI giữa các batch. |
| TC_CALC_007 | calculatePIHistories | [High][Edge] Calculation không cộng duplicate row sau Merge/FINAL | Cơ chế Merge/FINAL đã chốt | 1. Chuẩn bị duplicate cùng PI/time với `updated_date` khác nhau.<br>2. Trigger Merge/FINAL.<br>3. Chạy calculation. | `{"product_item_id":"test_calc_dup_001","old_total_sold":120,"new_total_sold":130,"last_total_sold":100}` | Calculation dùng row mới nhất, không cộng cả hai row duplicate; `sold` không bị phóng đại. |
| TC_CALC_008 | calculatePIHistories | [High][Parity] So sánh sold/GMV ClickHouse với Timescale trên cùng PI/time range | QA có quyền query Timescale và ClickHouse | 1. Chọn 10-20 PI mẫu gồm sold tăng, sold giảm, null price.<br>2. Query ClickHouse.<br>3. Query Timescale.<br>4. So sánh output. | `{"range":"2026-Q1","sampleSize":"10-20 PI","metrics":["sold","gmv","avg_price","avg_sell_price","discount"]}` | Sai lệch nằm trong parity threshold đã chốt. Sai lệch vượt threshold phải log bug hoặc block sign-off. |
| TC_CALC_009 | calculatePIHistories | [Medium][Boundary] Calculation không lệch ngày/tuần khi data VN/TL sát 00:00 | Timezone đã chốt | 1. Tạo/chọn data sát 00:00 và 23:59 cho VN/TL.<br>2. Chạy calculation theo ngày/tuần.<br>3. So sánh expected partition/time range. | `{"vn":"2026-07-06 00:00:00","tl":"2026-07-06 00:00:00","nearEndDay":"2026-07-06 23:59:59"}` | Record được tính vào đúng ngày/tuần, không lệch timezone hoặc partition. |
| TC_CALC_010 | calculatePIHistories | [Medium][Performance] Batch 1000 chạy trong threshold đã chốt | Có data Q1/2026+ và threshold đã chốt | 1. Chạy calculation batch 1000 PI.<br>2. Đo duration.<br>3. Kiểm tra CPU/memory/log timeout. | `{"range":"2026-Q1+","limit":1000,"threshold":"Need Confirm, đề xuất <5 phút"}` | Job hoàn tất trong threshold; không timeout; không spike tài nguyên bất thường. |
| TC_PARITY_001 | Data Parity | [High][Positive] Count parity theo tuần giữa Timescale và ClickHouse | Data Q1/2026+ đã load ở cả hai nguồn | 1. Chạy query count theo tuần trên ClickHouse.<br>2. Chạy query tương ứng trên Timescale.<br>3. So sánh count từng tuần. | `{"from":"2026-01-01","to":"2026-04-01","groupBy":"week"}` | Count theo tuần khớp hoặc lệch trong threshold đã chốt; mọi lệch đều có lý do và evidence. |
| TC_PARITY_002 | Data Parity | [High][Positive] Aggregate parity theo platform/crawler_type | Có data nhiều platform/crawler type | 1. Count/group theo `crawler_type` hoặc platform/source.<br>2. So sánh Timescale vs ClickHouse.<br>3. Kiểm tra nhóm thiếu bất thường. | `{"groupBy":["crawler_type","source_id"],"range":"2026-Q1+"}` | Không thiếu crawler type/source; count hợp lý; lệch vượt threshold phải log bug data migration. |
| TC_PARITY_003 | Data Parity | [Medium][Edge] Parity cho PI có nhiều record trong cùng ngày | Có PI crawl nhiều lần/ngày | 1. Chọn PI có N record/ngày.<br>2. So sánh raw count giữa hai nguồn.<br>3. So sánh aggregate sold/GMV. | `{"product_item_id":"PI nhiều record/ngày","date":"2026-07-06"}` | ClickHouse giữ đúng nhiều record trong ngày; aggregate không sai do nhiều lần crawl. |
| TC_PARITY_004 | Data Parity | [Medium][Negative] Phát hiện missing partition hoặc missing shard | QA có quyền query `system.parts` | 1. Count theo partition ở ClickHouse.<br>2. Tìm partition trống bất thường.<br>3. So sánh với Timescale cùng range. | `{"table":"eci.product_item_histories","range":"2026-Q1+"}` | Không thiếu partition/shard. Nếu thiếu data, block parity/sign-off cho range liên quan. |
| TC_CONCURRENT_001 | Concurrent Behavior | [High][Edge] Pusher insert trong khi calculation đang chạy không làm sai output | Có thể trigger pusher và calculation song song | 1. Start calculation job.<br>2. Trong lúc job chạy, insert batch mới vào ClickHouse.<br>3. Kiểm tra output calculation và log. | `{"batch":"new_records_during_calculation","product_item_id":"test_concurrent_001"}` | Output nhất quán theo design snapshot/Merge/FINAL; không cộng duplicate hoặc row chưa ổn định ngoài ý muốn. |
| TC_CONCURRENT_002 | Concurrent Behavior | [Medium][Edge] Hai pusher instance insert cùng PI được dedup đúng | Có 2 worker hoặc simulate song song | 1. Trigger hai insert cùng PI/time.<br>2. Trigger Merge/FINAL.<br>3. Query final output và aggregate. | `{"product_item_id":"test_concurrent_dup_001","same_crawled_date":true,"different_updated_date":true}` | Row `updated_date` mới hơn thắng; aggregate không nhân đôi. |
| TC_CONCURRENT_003 | Concurrent Behavior | [Medium][Performance] Query trong lúc Merge đang chạy không lỗi/timeout vượt ngưỡng | Có thể trigger Merge/Optimize trên dataset vừa đủ | 1. Start Merge/Optimize.<br>2. Chạy `checkInvalidRecords`.<br>3. Chạy `calculatePIHistories`.<br>4. Đo duration/log. | `{"operation":"OPTIMIZE or Merge job","queries":["checkInvalidRecords","calculatePIHistories"]}` | Query không lỗi; duration không vượt threshold đã chốt; nếu bị lock/chậm nghiêm trọng phải log bug hoặc đổi strategy. |
| TC_PUSHER_001 | Pusher PW/PM | [High][Positive] Pusher weekly nhận output calculation từ ClickHouse | `YNMPECA-9285` ở trạng thái To be Tested | 1. Chạy calculation weekly.<br>2. Theo dõi pusher output.<br>3. Query đích weekly. | `{"product_item_id":"test_weekly_001","delta_sold":20,"gmv":300000}` | Weekly output có sold/GMV đúng với calculation ClickHouse, không còn lấy sai nguồn. |
| TC_PUSHER_002 | Pusher PW/PM | [High][Positive] Pusher monthly nhận output calculation từ ClickHouse | `YNMPECA-9285` ready | 1. Chạy calculation monthly.<br>2. Query output monthly.<br>3. So sánh với expected/Timescale baseline. | `{"range":"2026-07","samplePI":"10 PI"}` | Monthly output đúng, không duplicate, sai lệch nằm trong threshold parity đã chốt. |
| TC_PUSHER_003 | Pusher PW/PM | [Medium][Regression] Re-run cùng calculation batch không tạo duplicate aggregate | Pusher PW/PM ready | 1. Chạy cùng job 2 lần.<br>2. Query output weekly/monthly.<br>3. So sánh count và aggregate. | `{"jobId":"same_job_replayed","range":"current_week"}` | Không nhân đôi sold/GMV; idempotency đúng theo design. |
| TC_PUSHER_004 | Raw Histories Pusher | [Medium][Conditional] Pusher raw ghi record vào ClickHouse đúng mapping | `YNMPECA-9286` ready | 1. Push/replay một record PI hợp lệ.<br>2. Query ClickHouse theo `product_item_id`.<br>3. Đối chiếu các field chính. | `{"product_item_id":"test_raw_001","title":"Sản phẩm test","total_sold":120,"price":20000,"sell_price":15000}` | ClickHouse có row mới; mapping field chính đúng; materialized `delta_sold`, `gmv` đúng. |
| TC_PUSHER_005 | Raw Histories Pusher | [Medium][Edge] Batch insert raw không mất record và xử lý nullable fields | `YNMPECA-9286` ready | 1. Push batch có record đủ field và record có null optional fields.<br>2. Query count và sample row.<br>3. Kiểm tra log lỗi. | `{"batchSize":100,"nullableFields":["title","price","sell_price","raw_brand_1"]}` | Count trong ClickHouse bằng số record hợp lệ; optional null không làm fail toàn batch. |
| TC_PUSHER_006 | Raw Histories Pusher | [Medium][Negative] Insert fail không ack mất message | Dev đã chốt ack/retry mechanism | 1. Simulate ClickHouse insert fail.<br>2. Theo dõi queue/log.<br>3. Khôi phục ClickHouse và kiểm tra retry/replay. | `{"simulate":"ClickHouse timeout","batchSize":10}` | Message không mất; được retry hoặc vào DLQ theo design; log đủ trace để recover. |
| TC_REALTIME_001 | Product Items Realtime | [Medium][Performance] Cập nhật 10.000 records trong 1 phút | `YNMPECA-9287` ready; có batch test 10.000 records | 1. Trigger batch update 10.000 records.<br>2. Đo thời gian xử lý.<br>3. Query số record đã cập nhật và failed count. | `{"batchSize":10000,"target":"Product Items","threshold":"1 phút"}` | Hoàn tất trong 1 phút hoặc threshold đã chốt; failed count = 0 hoặc retry thành công theo design. |
| TC_REALTIME_002 | Data Freshness | [Medium][Integration] Đo freshness từ pusher tới ClickHouse, Product Items và App | SLA đã chốt hoặc dùng threshold tạm thời | 1. Ghi timestamp khi record vào pusher.<br>2. Query ClickHouse.<br>3. Query Product Items.<br>4. Call App/API để thấy data mới. | `{"product_item_id":"test_freshness_001","eventTime":"now()"}` | Thời gian end-to-end nhỏ hơn SLA đã chốt; nếu chưa chốt SLA, ghi Need Confirm và dùng số đo làm baseline. |
| TC_APP_001 | App Adapter | [Medium][Positive] API/App load Product Item Histories từ ClickHouse đúng dữ liệu | `YNMPECA-9288` ready | 1. Call API/tool sold histories cho PI test.<br>2. Query ClickHouse cùng PI/time range.<br>3. So sánh response. | `{"product_item_id":"test_app_1y_001","range":"1 năm"}` | API trả dữ liệu từ ClickHouse đúng sold, sell price, title, `crawled_date`; không crash. |
| TC_APP_002 | App Adapter | [Medium][Performance] Load lịch sử 1 năm không timeout | App adapter ready; threshold đã chốt | 1. Call API với range 1 năm.<br>2. Đo response time.<br>3. Kiểm tra số record trả về và log. | `{"product_item_id":"PI có lịch sử 1 năm","threshold":"Need Confirm, đề xuất <5 giây"}` | Response time nhỏ hơn threshold; dữ liệu đầy đủ; nếu timeout/chậm phải log bug performance. |
| TC_LEGACY_001 | Legacy Fields | [Medium][Regression] Remove `crawled_history`, `sold_history` không làm pusher/App crash | `YNMPECA-9295` ready; App tool sold histories đã sửa xong | 1. Deploy theo đúng thứ tự đã confirm.<br>2. Push một record PI.<br>3. Call tool/API sold histories.<br>4. Kiểm tra log resolver/pusher/App. | `{"removedFields":["crawled_history","sold_history"],"product_item_id":"test_legacy_001"}` | Resolver/data pusher/App không crash vì thiếu field cũ; sold histories lấy từ ClickHouse. |
| TC_FLAG_001 | Feature Flag / Rollback | [Medium][Conditional] Rollback datasource từ ClickHouse về Timescale không mất dữ liệu | Có feature flag hoặc runbook rollback | 1. Chạy batch với ClickHouse ON.<br>2. Rollback/tắt ClickHouse datasource theo runbook.<br>3. Chạy batch khác.<br>4. Kiểm tra output và log datasource. | `{"batch1":"ClickHouse ON","batch2":"Timescale fallback"}` | Không mất batch; không duplicate ngoài ý muốn; log thể hiện datasource từng batch. Nếu không có fallback, job phải fail an toàn. |
| TC_ROLLBACK_001 | Rollback / Fallback | [Medium][Negative] Rollback khi parity sai vượt threshold | Dev/PM đã chốt rollback action | 1. Simulate hoặc phát hiện parity sai vượt threshold.<br>2. Thực hiện rollback theo runbook.<br>3. Chạy smoke lại datasource fallback. | `{"scenario":"calculation parity vượt threshold","threshold":"đã chốt ở TC_READY_004"}` | Service quay về datasource an toàn trong thời gian đã chốt; không tiếp tục phát sinh output sai. |
| TC_ROLLBACK_002 | Rollback / Fallback | [Medium][Negative] ClickHouse unavailable không làm mất message hoặc ghi sai dữ liệu | Có thể simulate ClickHouse down/timeout | 1. Simulate ClickHouse unavailable.<br>2. Trigger query hoặc pusher batch.<br>3. Kiểm tra queue/job/log.<br>4. Khôi phục ClickHouse và retry nếu có. | `{"simulate":"ClickHouse down","batchSize":10}` | Nếu có fallback thì dùng Timescale; nếu không có fallback thì job fail an toàn, không ack mất message và không ghi sai dữ liệu. |
| TC_DQ_001 | Conditional Scope | [Low][Conditional] Xác nhận Module 9 in-scope hoặc out-of-scope | Có PM/Dev trong NC meeting | 1. Hỏi scope duplicate sold islands.<br>2. Hỏi scope `findBrokenLastSoldChain`.<br>3. Hỏi scope rải số từ ClickHouse.<br>4. Cập nhật execution plan. | `{"items":["duplicate sold islands","findBrokenLastSoldChain","rải số từ ClickHouse"]}` | Module 9 được gắn rõ In-scope, Out-of-scope hoặc tạo task riêng; không để QA execute mơ hồ. |
| TC_DQ_002 | Data Quality Diagnostics | [Low][Conditional] Phát hiện duplicate sold islands nếu Module 9 in-scope | PM/Dev confirm Module 9 in-scope | 1. Chuẩn bị PI có 3 record liên tiếp cùng `total_sold`.<br>2. Chạy query diagnostic.<br>3. Kiểm tra output và duration. | `{"product_item_id":"test_dq_island_001","same_total_sold_count":3}` | Query trả đúng PI/group có `group_size >= 3`; duration nằm trong benchmark đã chốt. |
| TC_DQ_003 | Data Quality Diagnostics | [Low][Conditional] Phát hiện broken last sold chain nếu Module 9 in-scope | PM/Dev confirm Module 9 in-scope | 1. Chuẩn bị PI có chain đúng và chain sai.<br>2. Chạy diagnostic.<br>3. Kiểm tra expected fields. | `{"product_item_id":"test_dq_chain_001","wrong_last_crawled_date":true,"wrong_last_total_sold":true}` | Output chỉ ra `expected_last_crawled_date`, `expected_last_total_sold` và flag sai đúng cho record lỗi. |
